# Codex Decisions

## 9. 数据导入先做本地预检工作台

**决定了什么：**

数据导入页第一阶段只在浏览器内读取 JSON 文件或粘贴文本，解析记录数量、字段完整度和敏感字段提醒，并明确标记“仅本地预检，暂不上传”。

**为什么这么决定：**

当前还没有受控导入 API，也不应让管理后台直连数据库或直接控制 crawler。先把 crawler 导出文件的结构预检和风险提示做成前端工作台，可以为后续 API 入库、字段映射和审计流程提供稳定边界。

**已否决方案：**

- 直接新增或假调用不存在的导入 API。
- 前端直连数据库写入业务数据。
- 前端直接控制本地 crawler 进程。
- 将 sessionId、Cookie、Authorization、原始 HTTP exchange 等敏感字段纳入上传流程。

## 7. 改密成功后强制重新登录

**决定了什么：**

系统设置页调用 `POST /api/admin/auth/change-password` 成功后，前端立即清理 `cekaitech-admin-token` 并跳转登录页。

**为什么这么决定：**

当前后台 token 是轻量签名 token，不具备服务端 session 主动失效能力。改密后强制本端重新登录，可以避免当前页面继续沿用旧会话操作，也能提醒操作者使用新密码验证。

**已否决方案：**

- 改密成功后继续停留在当前登录态。
- 在前端本地保存新密码或其他敏感信息。

## 6. 管理后台首版按静态站部署

**决定了什么：**

`cekaitech-admin` 首版构建为 Vite 静态产物，部署到 `admin.cekaitech.cn`，由 Nginx 托管；生产 API 地址通过 `VITE_API_BASE_URL=https://api.cekaitech.cn` 在构建期注入。

**为什么这么决定：**

当前后台是纯前端管理壳，运行时只需要浏览器访问和调用 `miniapp-backend` 受控 API。静态部署成本低、回滚简单，也能与后端 `api.cekaitech.cn` 的服务进程边界保持清晰。

**已否决方案：**

- 将后台前端打包进 `miniapp-backend` 一起部署。
- 在前端生产环境变量中保存 secret。
- 让后台直连数据库或绕过后端 API。

## 5. 登录失效由 HTTP 层派发事件

**决定了什么：**

`src/api/http.ts` 在非登录接口收到 HTTP 401 时清理本地后台 token，并派发 `cekaitech-admin:unauthorized` 事件；应用入口监听该事件后跳转登录页。

**为什么这么决定：**

HTTP 工具层是所有后端请求的统一入口，适合收敛 401 处理；但它不应直接依赖 Pinia store 或路由实例做复杂业务编排。事件方式可以保持 API 层轻量，同时让应用入口负责页面跳转。

**已否决方案：**

- 在每个页面单独捕获 401 并跳转登录页。
- 让 `http.ts` 直接依赖 auth store 并操作路由。

## 1. 管理后台独立于小程序目录

**决定了什么：**

`cekaitech-admin` 放在 `C:\Users\p15-gen2\Documents\work_space\myself\java\cekaitech-admin`，不放在 `miniapp` 目录下。

**为什么这么决定：**

管理后台是公司级运营系统，不属于某一个小程序前端。放在 `java` 工作区可以和本地 `crawler`、后端管理能力保持更清晰的工程边界。

**已否决方案：**

- 放在 `miniapp/cekaitech-admin` 作为小程序矩阵子目录。

## 2. 管理后台只通过后端 API 管理业务数据

**决定了什么：**

后台不直接写生产数据库，不直接操作小程序本地缓存，不直接控制本地 crawler。

**为什么这么决定：**

所有生产业务事实需要经过 `miniapp-backend` 的权限、校验、审计和风控边界，避免后台和数据采集能力绕过统一后端。

**已否决方案：**

- 后台直连数据库修改业务表。
- 后台直接调用本地 crawler 进程写云端数据。

## 3. 首版后台框架优先

**决定了什么：**

`cekaitech-admin` 首版先搭后台壳，包括登录占位、主布局、左侧菜单、应用/租户切换和业务占位页面。

**为什么这么决定：**

后台会承载多个小程序和公司级运营能力。先建立统一信息架构和扩展方式，可以降低后续用户管理、律师认证、订单、数据导入等模块接入成本。

**已否决方案：**

- 首版直接做单一业务页面。
- 首版直接接生产接口。

## 4. 使用 Vue 3 + Vite + TypeScript + Element Plus

**决定了什么：**

首版前端技术栈采用 Vue 3 + Vite + TypeScript + Element Plus。

**为什么这么决定：**

该组合适合后台管理系统，表格、表单、菜单、弹窗等基础组件成熟，开发速度快，适合当前一个人 + AI 的迭代模式。

**已否决方案：**

- React + Ant Design。
- 暂不定框架只继续写文档。

## 8. 前端权限控制以权限码为最小单元

**决定了什么：**

前端使用登录接口返回的 `operator.permissions` 作为菜单、路由和按钮可见性的唯一判断来源。全局菜单、工作区菜单、路由 meta 和高风险按钮都绑定具体 `permissionCode`。

**为什么这么决定：**

后端已经用同一组权限码保护接口，前端保持相同口径可以减少“页面可见但接口 403”或“页面隐藏但接口可调”的错位。第一阶段先做可见性和基础路由拦截，不引入复杂前端角色模型。

**已否决方案：**

- 只依赖后端 403，不在前端隐藏入口。
- 在前端维护独立角色枚举而不是复用后端权限码。
- 第一阶段新增完整权限配置页面。
