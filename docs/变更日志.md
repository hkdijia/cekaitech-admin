# 变更日志

本文档记录 `cekaitech-admin` 项目内已经落地的功能、配置、脚本和文档变更。

## 2026-05-24

- [反馈编号：无] 后台服务请求详情联系方式查看改为显式动作：普通详情和状态更新响应默认展示 `contactPhoneMasked`，点击“查看完整手机号”后调用 `POST /api/admin/legal/service-requests/{requestId}/contact-view` 获取完整 `contactPhone`，适配后端审计接口；查看失败在详情联系人区域提示错误。
- [反馈编号：无] 同步管理后台项目状态：`AGENTS.md`、`tasks/current-task.md` 和 `codex-handoff.md` 从占位/已完成未提交修正为后台 MVP+ 已提交已推送，等待真实后端联调和下一阶段业务页。
- [反馈编号：无] 用户管理页空筛选归一化：空 `keywords`、`status`、`appCode` 下发 `undefined`，继续保留合法 `userId` 精确筛选和 JS 安全整数边界；`UsersPage.test.ts` 覆盖空筛选不下发和合法 userId 仍下发。
- [反馈编号：无] 补齐管理后台反馈编号追溯约束：更新 `AGENTS.md`，要求来自企业微信文档或产品反馈台账的改动在任务文档、变更日志、验证记录、提交信息和阶段回写中保留同一反馈编号；无产品反馈编号的工程维护写 `Refs: none`。
- 新增服务请求管理页 `/legal-service-requests`，通过 `miniapp-backend` 的 `POST /api/admin/legal/service-requests/page` 查询服务请求，不直连数据库、不控制 crawler。
- 新增 `src/api/legalServiceRequests.ts`，封装服务请求分页、详情和状态更新接口；详情接口读取完整手机号，状态更新调用 `POST /api/admin/legal/service-requests/{requestId}/status`。
- 主菜单和路由新增“服务请求”，权限码 `admin:legal-service-request:view`；管理操作入口按 `admin:legal-service-request:manage` 控制。
- 服务请求页支持关键词、用户 ID、手机号、服务类型、状态和小程序筛选；空筛选统一传 `undefined`，用户 ID 仅正整数且 `Number.isSafeInteger` 时下发。
- 服务请求页展示请求 ID、用户 ID、小程序、服务类型、联系人、脱敏手机号、状态、处理人、创建/更新时间和详情操作；详情抽屉展示完整手机号、用户备注、内部备注、来源记录，并支持跳转 `/users?userId=...` 和 `/generation-records?userId=...`。
- 新增 `src/api/legalServiceRequests.test.ts`、`src/pages/legal-service-requests/LegalServiceRequestsPage.test.ts`，扩展 `src/router/router.test.ts`，按 TDD 覆盖 API 契约、菜单/路由权限、筛选归一化、非法用户 ID、查看用户跳转和管理权限状态更新。
- 验证通过：红灯 `npm.cmd run test -- --run src/api/legalServiceRequests.test.ts src/pages/legal-service-requests/LegalServiceRequestsPage.test.ts src/router/router.test.ts` 失败于缺少 API/页面模块和路由菜单声明；绿灯同命令通过，3 个测试文件、22 个 Vitest 测试通过；最终 `npm.cmd run quality` 通过，17 个测试文件、66 个 Vitest 测试通过，`vue-tsc --noEmit && vite build` 通过，保留既有 Rollup PURE 注释 warning 和 chunk size warning。
- 用户管理页将 `/users?userId=...` 从“关键词排查”升级为后端 `userId` 精确筛选：合法正整数且不超过 JS 安全整数范围的 query 会填入独立用户 ID 筛选框，请求 `POST /api/admin/users/page` 时下发 `userId`，并保持 `keywords` 为空。
- `AdminUserPageQuery` 增加 `userId?: number`，`src/api/adminUsers.test.ts` 覆盖分页请求体携带精确用户 ID。
- `UsersPage` 新增“用户ID”筛选项，保留关键词、状态、小程序筛选；非法、空白、小数、负数、超出 JS 安全整数范围和空数组 `userId` query 不下发 `userId`，避免污染查询条件或静默丢精度。
- 更新 `UsersPage.test.ts`，覆盖合法 query、数组 query、非法 query、关键词不污染和页面提示文案。
- 验证通过：`npm.cmd run test -- --run src/pages/users/UsersPage.test.ts src/api/adminUsers.test.ts`，2 个测试文件、13 个测试通过；相关定向 4 个测试文件、23 个测试通过；`npm.cmd run quality`，15 个测试文件、52 个测试通过，`vue-tsc --noEmit && vite build` 通过，保留既有 Rollup PURE 注释 warning 和 chunk size warning。

## 2026-05-23

- 生成记录页和法律表单事件页在用户 ID 列新增“查看用户”动作，跳转 `/users?userId=...` 作为跨页排查入口；不改生成记录、法律表单事件或用户分页后端接口契约。
- 用户管理页读取 `/users?userId=...` 查询参数，仅在合法正整数时填入现有 `keywords` 条件查询用户列表，并展示“关键词排查”提示，避免假造后端 `userId` 精确筛选能力。
- 扩展 `src/pages/generation-records/GenerationRecordsPage.test.ts`、`src/pages/legal-form-events/LegalFormEventsPage.test.ts`，新增 `src/pages/users/UsersPage.test.ts`，按 TDD 覆盖来源页跳转、用户页 query 初始化排查入口，以及非法、空白、小数、负数和数组 `userId` query 边界。
- 追加定向验证：`npm.cmd run test -- --run src/pages/users/UsersPage.test.ts src/pages/generation-records/GenerationRecordsPage.test.ts src/pages/legal-form-events/LegalFormEventsPage.test.ts` 通过，3 个测试文件、18 个 Vitest 测试通过。
- 验证通过：红灯 `npm.cmd run test -- --run src/pages/generation-records/GenerationRecordsPage.test.ts src/pages/legal-form-events/LegalFormEventsPage.test.ts src/pages/users/UsersPage.test.ts` 失败于缺少“查看用户”跳转和 query 关键词初始化；绿灯同命令通过，3 个测试文件、11 个 Vitest 测试通过；相关定向 `npm.cmd run test -- --run src/pages/generation-records/GenerationRecordsPage.test.ts src/pages/legal-form-events/LegalFormEventsPage.test.ts src/pages/users/UsersPage.test.ts src/router/router.test.ts src/api/adminUsers.test.ts` 通过，5 个测试文件、23 个 Vitest 测试通过；最终 `npm.cmd run quality` 通过，15 个测试文件、51 个 Vitest 测试通过，TypeScript 与 Vite 构建通过；构建保留既有 Rollup 注释 warning 和 chunk size warning。
- 后台路由守卫补齐刷新恢复：本地存在 `cekaitech-admin-token` 但 Pinia `operator` 缺失时，先调用已有 `getCurrentOperator` / `/api/admin/auth/current-operator` 恢复 operator 与 permissions，再执行权限判断；恢复失败时清理 token 并回到登录页。
- 法律表单事件页补充组件测试 `src/pages/legal-form-events/LegalFormEventsPage.test.ts`，覆盖初始加载空筛选归一化、关键词/用户 ID/状态/事件类型/小程序筛选和非法用户 ID。
- 法律表单事件页查询参数对齐生成记录页：空关键词、小程序、表单类型、质量状态和事件类型统一传 `undefined`，用户 ID 仅正整数下发；新增用户 ID 和事件类型筛选控件，不改后端接口路径。
- 验证通过：`npm.cmd run test -- --run src/stores/auth.test.ts src/api/adminAuth.test.ts src/api/http.test.ts src/api/legalFormEvents.test.ts src/pages/legal-form-events/LegalFormEventsPage.test.ts src/router/router.test.ts` 通过，6 个测试文件、18 个 Vitest 测试通过；`npm.cmd run quality` 通过，14 个测试文件、41 个 Vitest 测试通过，TypeScript 与 Vite 构建通过；构建保留既有 Rollup 注释 warning 和 chunk size warning。
- 生成记录页补充组件交互测试 `src/pages/generation-records/GenerationRecordsPage.test.ts`，mock 分页 API 覆盖初始加载、列表字段渲染、筛选查询、重置和非法用户 ID 场景。
- 生成记录页查询参数归一化：空关键词、小程序、状态和记录类型统一传 `undefined`，用户 ID 仅正整数传给后端，避免空字符串和 `NaN` 影响联调。
- 生成记录类型筛选对齐法律助手小程序实际 `caseType`：补齐 `divorce`、`labor`、`contract`、`tort` 展示与筛选，避免通用模板云端记录在后台只能显示原始枚举。
- 验证通过：`npm.cmd run test -- --run src/pages/generation-records/GenerationRecordsPage.test.ts` 通过，1 个测试文件、5 个 Vitest 测试通过；`npm.cmd run test -- --run src/pages/generation-records/generationRecordOptions.test.ts` 通过，3 个测试通过；`npm.cmd run quality` 通过，13 个测试文件、36 个 Vitest 测试通过，TypeScript 与 Vite 构建通过；构建保留既有 Rollup 注释 warning 和 chunk size warning。
- 新增生成记录后台页面 `/generation-records`，通过 `miniapp-backend` 的 `POST /api/admin/generation-records/page` 查询法律助手小程序云端生成记录，不直连数据库、不接 crawler。
- 新增 `src/api/generationRecords.ts`，封装生成记录分页接口；请求字段使用 `pageNo`、`pageSize`、`appCode`、`userId`、`status`、`recordType`、`keywords`、`orderBy`、`order`，返回沿用 `dataList`、`totalCount`。
- 新增 `src/api/generationRecords.test.ts`，按 TDD 覆盖生成记录分页接口请求路径、请求体和响应解析。
- 主菜单和路由新增“生成记录”，路径 `/generation-records`，权限码 `admin:generation-record:view`；扩展路由测试覆盖入口权限声明。
- 生成记录页支持关键词、用户 ID、状态、记录类型和小程序筛选，展示用户 ID、身份 ID、小程序、客户端记录 ID、标题、类型、状态、摘要、创建时间和更新时间。
- 生成记录页状态选项按后端契约修正为 `draft`（待填写/草稿）、`generated`（已生成）、`expired`（已过期），移除不匹配的 `processing/success/failed`。
- 生成记录页记录类型按小程序本机 `caseType` 口径修正，覆盖 `private_lending`、`divorce_agreement`、`generic_template`、`contract_template`，未知值兜底显示原值。
- 新增 `src/pages/generation-records/generationRecordOptions.ts` 和测试，集中维护生成记录状态、tag 类型和记录类型展示映射。
- 分页选择限制在 `10/20/50/100`，前端请求时按后端约束将 `pageSize` 收敛到最大 100。
- 验证通过：`npm.cmd run quality` 通过，12 个测试文件、31 个 Vitest 测试通过，TypeScript 与 Vite 构建通过；构建保留既有 Rollup 注释 warning 和 chunk size warning。
- 新增法律表单事件后台页面 `/legal-form-events`，通过 `miniapp-backend` 的 `POST /api/admin/legal/form-events/page` 查询表单事件，不直连数据库。
- 新增 `src/api/legalFormEvents.ts`，封装法律表单事件分页接口；请求字段使用 `pageNo`、`pageSize`、`orderBy`、`order`、`appCode`、`formType`、`qualityStatus`、`keywords`，返回沿用 `dataList`、`totalCount`。
- 新增 `src/api/legalFormEvents.test.ts`，按 TDD 覆盖法律表单事件分页接口请求路径、请求体和响应解析。
- 主菜单和路由新增“法律表单事件”，路径 `/legal-form-events`，权限码 `admin:legal-form-event:view`；扩展路由测试覆盖入口权限声明。
- 法律表单事件页支持关键词、表单类型、质量状态和小程序筛选，展示分页表格、事件基础字段和 `payloadPreview` 预览列。
- 表单类型和质量状态筛选按当前小程序实际上报值配置，覆盖 `private_lending`、`divorce_agreement`、`generic_template` 和 `valid/low_value`。
- 关键词提示收敛为当前后端实际支持的客户端事件 ID、事件类型和 payload 内容，避免误导为可按用户 ID 搜索。
- 验证通过：`npm.cmd run quality` 通过，10 个测试文件、26 个 Vitest 测试通过，TypeScript 与 Vite 构建通过；构建保留既有 Rollup 注释 warning 和 chunk size warning。
- 同步平台基线 checkpoint：记录当前 HEAD `cf22873`，以及 `npm.cmd run quality` 通过，8 个测试文件、21 个 Vitest 测试通过，TypeScript 与 Vite 构建通过。
- 数据导入页从占位升级为本地预检工作台，支持选择 JSON 文件或粘贴 JSON 文本，在浏览器内解析记录数量、顶层字段完整度和敏感字段提醒。
- 新增 `src/pages/data-import/importPreflight.ts`，封装本地 JSON 预检纯函数；支持常见导出结构 `root[]`、`records[]`、`items[]`、`dataList[]`、`list[]` 和 `data[]`。
- 新增 `src/pages/data-import/importPreflight.test.ts`，覆盖记录数量、字段完整度、敏感字段检测和非法 JSON 状态。
- 数据导入页明确显示“仅本地预检，暂不上传”，当前不新增后端 API、不直连数据库、不控制 crawler。
- 验证通过：`npm.cmd run quality` 通过，9 个测试文件、24 个 Vitest 测试通过，TypeScript 与 Vite 构建通过；构建保留既有 Vite/Rollup warning 和 chunk size warning。

## 2026-05-21

- 初始化 `cekaitech-admin` 占位仓库。
- 新增项目级 `AGENTS.md`、`README.md`、checkpoint 文档和变更日志。
- 明确管理后台只通过 `miniapp-backend` 受控 API 管理业务数据，不直接写生产数据库，不直接控制本地 `crawler`。

## 2026-05-21

- 确认 `cekaitech-admin` 首版采用后台框架优先策略。
- 确认技术栈为 Vue 3 + Vite + TypeScript + Element Plus。
- 新增 `docs/admin-mvp-design.md`，明确首批页面、目录结构、数据边界、权限阶段和暂不做范围。
- 更新 checkpoint 文档，记录当前处于设计复核阶段。

## 2026-05-21

- 新增 `docs/superpowers/plans/2026-05-21-admin-mvp-shell.md`，作为 `cekaitech-admin` MVP 后台壳的实现计划。

## 2026-05-21

- 初始化 Vue 3 + Vite + TypeScript + Element Plus 后台壳。
- 新增登录占位、后台主布局、左侧菜单、应用/工作区切换和首页工作台。
- 新增用户管理、限制与黑名单、律师认证审核、数据导入、系统设置占位页面。
- 新增路由、mock 登录状态、工作区状态和基础测试。
- 新增本地开发、测试、构建和质量检查命令说明。

## 2026-05-22

- 新增前端权限判断能力：`auth.hasPermission(permissionCode)` 基于当前管理员权限数组判断可见性。
- 左侧全局菜单新增 `permissionCode`，通过 `filterAdminMenuItems` 按权限过滤。
- 后台路由新增 `meta.permissionCode`，路由守卫在已登录但无权限时重定向到 `/dashboard`。
- 工作区菜单新增 `filterWorkspaceMenus`，按后端返回的 `permissionCode` 过滤当前可见工作区功能。
- 用户管理页按权限控制“生成演示数据”和“状态调整”按钮：分别对应 `admin:dev:seed` 与 `admin:user:status:update`。
- 限制与黑名单页按 `admin:user-restriction:manage` 控制“新增限制”和“取消”按钮。
- 扩展 auth、router、workspace 测试，覆盖权限判断、全局菜单过滤、路由权限声明和工作区菜单过滤。
- 验证通过：`npm.cmd run quality` 通过，21 个 Vitest 测试通过，TypeScript 与 Vite 构建通过。
- 系统设置页新增当前管理员改密表单，调用 `POST /api/admin/auth/change-password`。
- 新增 `changeAdminPassword` API 封装和 `src/api/adminAuth.test.ts`，覆盖改密请求携带 Bearer token。
- 改密成功后清理本地后台 token 并跳转登录页，要求管理员使用新密码重新登录。
- 更新 `codex-decisions.md`，记录改密成功后强制重新登录的决策。
- 验证通过：`npm.cmd run quality` 通过，17 个 Vitest 测试通过，TypeScript 与 Vite 构建通过。
- 新增 `.env.production.example`，生产构建默认指向 `https://api.cekaitech.cn`。
- 新增 `docs/production-runbook.md`，记录 `admin.cekaitech.cn` 静态部署、Nginx、验证、回滚和安全边界。
- 更新 `codex-decisions.md`，记录管理后台首版按 Nginx 静态站部署的决策。
- 更新 README，补充生产环境变量和部署文档入口。
- 通用请求封装新增后台登录失效处理：非登录接口收到 HTTP 401 时清理 `cekaitech-admin-token`，并派发 `cekaitech-admin:unauthorized` 事件。
- 应用入口监听登录失效事件，当前不在登录页时自动跳转 `/login`，避免后台全局鉴权落地后用户停留在失效会话页面。
- 新增 `src/api/http.test.ts`，覆盖受保护接口 401 清理 token 和登录接口 401 不触发全局失效处理。
- 验证通过：`npm.cmd run quality` 通过，16 个 Vitest 测试通过，TypeScript 与 Vite 构建通过。
- 通用请求封装 `src/api/http.ts` 新增后台 token 自动注入：本地存在 `cekaitech-admin-token` 时，除登录接口外自动附带 `Authorization: Bearer <token>`。
- 用户状态调整请求会携带登录 token，支撑 `miniapp-backend` 将真实后台管理员写入用户操作审计日志。
- 扩展 `src/api/adminUsers.test.ts`，覆盖状态调整请求携带 Authorization 头。
- 验证通过：`npm.cmd run quality` 通过，14 个 Vitest 测试通过，TypeScript 与 Vite 构建通过。
- 新增 `src/api/adminUserOperationLogs.ts`，封装 `POST /api/admin/user-operation-logs/page` 后台用户操作日志分页接口。
- 用户管理详情抽屉新增“操作记录”区域，进入详情后加载用户最近状态调整记录，状态调整成功后自动刷新记录。
- 新增 `src/api/adminUserOperationLogs.test.ts`，覆盖操作日志分页接口请求。
- 验证通过：`npm.cmd run quality` 通过，14 个 Vitest 测试通过，TypeScript 与 Vite 构建通过。
- 用户管理页新增“调整状态”操作，可将用户设为正常、受限或黑名单，并要求填写原因。
- 新增 `updateAdminUserStatus` API 封装，调用 `POST /api/admin/users/update-status` 并刷新用户列表/详情。
- 扩展 `src/api/adminUsers.test.ts`，覆盖用户状态调整接口请求。
- 验证通过：`npm.cmd run quality` 通过，13 个 Vitest 测试通过，TypeScript 与 Vite 构建通过。
- 限制与黑名单页面从占位页升级为用户限制管理页面，调用 `miniapp-backend` 用户限制接口。
- 新增 `src/api/adminUserRestrictions.ts`，封装限制记录分页、创建和取消接口。
- 限制管理页支持按用户ID、小程序、限制类型和状态筛选，支持新增全局/指定小程序限制，支持取消生效中限制。
- 新增 `src/api/adminUserRestrictions.test.ts`，覆盖分页、创建和取消接口请求。
- 验证通过：`npm.cmd run quality` 通过，12 个 Vitest 测试通过，TypeScript 与 Vite 构建通过。
- 用户管理页新增“生成演示数据”按钮，调用 `POST /api/admin/dev/seed-users` 后刷新列表，便于本地联调空库场景。
- 用户管理页新增详情抽屉，调用 `GET /api/admin/users/{userId}/detail` 展示用户基础信息、小程序身份列表和手机号记录。
- 扩展 `src/api/adminUsers.ts` 和 `src/api/adminUsers.test.ts`，覆盖用户分页、用户详情和演示数据生成接口。
- 验证通过：`npm.cmd run quality` 通过，10 个 Vitest 测试通过，TypeScript 与 Vite 构建通过。
- 用户管理页从占位页升级为真实 API 页面，调用 `POST /api/admin/users/page` 加载统一用户列表。
- 新增 `src/api/adminUsers.ts`，封装后台用户分页查询接口和分页返回结构。
- 用户管理页支持关键词、用户状态和来源小程序筛选，并展示手机号、`unionId`、`openid`、手机号授权、角色和创建时间。
- 新增 `src/api/adminUsers.test.ts`，覆盖用户分页 API 的请求路径、请求体和分页响应解析。
- 前端登录从纯 mock store 改为调用 `miniapp-backend` 的后台开发态登录接口。
- 新增 `src/api/http.ts` 和 `src/api/adminAuth.ts`，统一封装后台 API 请求和管理员认证接口。
- 登录成功后保存 `cekaitech-admin-token`，登出时清理本地 token。
- Vite 开发服务新增 `/api -> http://127.0.0.1:8080` 代理，便于本地前后端联调。
- 更新 auth store 测试，覆盖 API 登录成功和登录失败提示。
- 工作区选择从前端固定数组升级为调用 `GET /api/admin/workspaces`，接口异常时保留前端兜底工作区。
- 新增 `src/api/adminWorkspace.ts`，封装工作区列表和工作区菜单接口。
- 切换工作区后调用 `GET /api/admin/workspaces/{workspaceId}/menus` 并在侧边栏展示工作区菜单区。
- 新增 workspace store 测试，覆盖工作区加载、兜底列表和菜单加载。
- 新增动态工作区菜单路由 `/workspace-menu/:workspaceCode/:menuCode`，侧边栏工作区菜单可点击进入通用功能占位页。
- 新增 `WorkspaceMenuPage`，展示工作区编码、菜单编码、后端路由和权限码，作为后续真实业务页面接入前的稳定落点。
