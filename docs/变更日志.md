# 变更日志

本文档记录 `cekaitech-admin` 项目内已经落地的功能、配置、脚本和文档变更。

## 2026-06-26

- [反馈编号：无] 门店预约 admin 门店资料配置完成组件拆分：新增 `StoreAppointmentStoreProfilePanel.vue`，将 `storeCode` 读取、门店中性展示字段编辑、保存和错误提示从 `StoreAppointmentsPage.vue` 中抽出。
- [反馈编号：无] 新增 `StoreAppointmentStoreProfilePanel.test.ts` 覆盖读取门店资料、保存中性字段并携带 `store-config-...` requestId、保存失败保留草稿、无 `admin:store-appointment-config:manage` 权限时隐藏读取/保存入口。
- [反馈编号：无] 本轮仅做前端结构收口和测试资产补强，不新增后端接口；门店资料仍只包含名称、行业、展示电话、地址、营业时间、员工称谓、项目称谓和展示价格开关，不接入支付、会员、核销、客户资料、员工账号或真实排班。
- [反馈编号：无] TDD 验证：RED 阶段组件测试失败于 `StoreAppointmentStoreProfilePanel.vue` 不存在；GREEN 后组件测试 1 个文件、4 项通过，页面测试 1 个文件、28 项通过，API/readiness/page/rollback/store-profile 组合 5 个文件、50 项通过。
- [反馈编号：无] 门店预约 admin 配置回滚 Flow 完成组件拆分：新增 `StoreAppointmentConfigRollbackPanel.vue`，将 `storeCode + auditLogId` 预览、显式勾选确认和受控执行逻辑从 `StoreAppointmentsPage.vue` 中抽出。
- [反馈编号：无] 新增 `StoreAppointmentConfigRollbackPanel.test.ts` 覆盖回滚预览不执行、执行前必须勾选确认、无 `admin:store-appointment-config:manage` 权限时隐藏预览/执行入口。
- [反馈编号：无] 本轮仅做前端结构收口和测试资产补强，不新增后端接口；仍要求管理员输入已知审计记录 ID，不引入审计列表假接口。
- [反馈编号：无] TDD 验证：RED 阶段组件测试失败于 `StoreAppointmentConfigRollbackPanel.vue` 不存在；GREEN 后组件测试 1 个文件、3 项通过，页面测试 1 个文件、28 项通过，API/readiness/page/component 定向组合 4 个文件、46 项通过。

## 2026-06-21

- [反馈编号：无] 门店预约 admin 配置页新增“配置回滚”Flow：具备 `admin:store-appointment-config:manage` 权限时，可按 `storeCode + auditLogId` 先调用只读预览，再勾选确认后执行受控回滚。
- [反馈编号：无] 新增回滚 API client：`GET /api/admin/store-appointment-config/stores/{storeCode}/rollback-preview/{auditLogId}` 用于预览，`POST /api/admin/store-appointment-config/stores/{storeCode}/rollback/{auditLogId}` 用于执行，执行请求生成 `store-config-...` requestId。
- [反馈编号：无] 回滚 Flow 明确“预览不写库”，执行会写入新的 rollback 审计；仅限门店资料、项目目录、员工名册和预约规则四个中性配置面，不恢复支付、会员、核销、客户资料、CRM、服务记录、员工账号或真实排班。
- [反馈编号：无] TDD 验证：RED 阶段 `storeAppointments.test.ts` 失败于回滚 API client 缺失，`check-admin-integration-ready.test.mjs` 失败于 readiness 未识别回滚 client，`StoreAppointmentsPage.test.ts` 失败于页面缺少“配置回滚”block 和 `回滚 storeCode` 输入；GREEN 后 API/readiness/page 定向组合 3 个文件、43 项通过。
- [反馈编号：无] 门店预约 admin 配置页新增“预约规则配置”block：具备 `admin:store-appointment-config:manage` 权限时，可按 `storeCode` 读取并编辑可约窗口天数、默认服务时长、默认时段、确认提示和取消提示。
- [反馈编号：无] 预约规则保存调用 `PUT /api/admin/store-appointment-config/rules/{storeCode}`，写请求生成 `store-config-...` requestId；保存成功回填后端响应，保存失败保留用户草稿输入。
- [反馈编号：无] 预约规则 block 仍只处理基础预约规则，不接入真实排班、消息通知、退款规则、客户账户策略、支付、会员、核销、CRM 或服务记录。
- [反馈编号：无] TDD 验证：RED 阶段 `StoreAppointmentsPage.test.ts` 失败于页面缺少“预约规则配置”block 和 `预约规则 storeCode` 输入；GREEN 后页面测试 1 个文件、25 项通过，API/readiness/page 定向组合 3 个文件、39 项通过。
- [反馈编号：无] 门店预约 admin 配置页新增“员工名册配置”block：具备 `admin:store-appointment-config:manage` 权限时，可按 `storeCode` 读取员工列表，选择单员工编辑员工姓名、角色、简介、头像 URL、员工亮点、可服务项目 code 和启用状态。
- [反馈编号：无] 员工保存调用 `PUT /api/admin/store-appointment-config/staff/{staffCode}`，请求体携带 `storeCode` 限定门店范围，并生成 `store-config-...` requestId；保存成功回填当前员工，保存失败保留用户草稿输入。
- [反馈编号：无] 员工名册 block 仍只处理中性员工展示字段和项目 code，不接入员工账号、权限、私联信息、真实排班、支付、会员、CRM、服务记录或客户资料。
- [反馈编号：无] TDD 验证：RED 阶段 `StoreAppointmentsPage.test.ts` 失败于页面缺少“员工名册配置”block 和 `员工名册 storeCode` 输入；GREEN 后页面测试 1 个文件、21 项通过，API/readiness/page 定向组合 3 个文件、35 项通过。
- [反馈编号：无] 门店预约 admin 配置页新增“项目目录配置”block：具备 `admin:store-appointment-config:manage` 权限时，可按 `storeCode` 读取项目列表，选择单项目编辑分类 code、项目名称、摘要、默认时长、展示价格文案、展示价格开关和启用状态。
- [反馈编号：无] 项目保存调用 `PUT /api/admin/store-appointment-config/projects/{projectCode}`，请求体携带 `storeCode` 限定门店范围，并生成 `store-config-...` requestId；保存成功回填当前项目，保存失败保留用户草稿输入。
- [反馈编号：无] 项目目录 block 仍只处理中性预约展示字段，`priceText` 仅为展示文案，不接入真实支付金额、定金、会员卡、核销、客户资料、CRM、员工账号或真实排班。
- [反馈编号：无] TDD 验证：RED 阶段 `StoreAppointmentsPage.test.ts` 失败于页面缺少“项目目录配置”block 和 `项目目录 storeCode` 输入；GREEN 后页面测试 1 个文件、17 项通过，API/readiness/page 定向组合 3 个文件、31 项通过。
- [反馈编号：无] 门店预约 admin 配置页新增“门店资料配置”首个可编辑 block：具备 `admin:store-appointment-config:manage` 权限时，可按 `storeCode` 读取并编辑门店名称、行业、展示电话、地址、营业时间、员工称谓、项目称谓和展示价格开关。
- [反馈编号：无] 门店资料保存调用 `PUT /api/admin/store-appointment-config/stores/{storeCode}`，写请求生成 `store-config-...` requestId；保存成功回填后端响应，保存失败保留用户草稿输入。
- [反馈编号：无] 无配置管理权限时仅展示权限提示，不显示读取和保存按钮；本轮仍不开放项目目录、员工名册、预约规则、回滚、支付、会员、核销、客户资料、CRM、服务记录、员工账号或真实排班。
- [反馈编号：无] TDD 验证：RED 阶段 `StoreAppointmentsPage.test.ts` 失败于页面缺少“门店资料配置”block 和 `配置 storeCode` 输入；GREEN 后页面测试 1 个文件、13 项通过，API/readiness/page 定向组合 3 个文件、27 项通过。
- [反馈编号：无] 门店预约 admin 配置新增 API client 切片：`src/api/storeAppointments.ts` 封装门店资料、项目目录、员工名册和预约规则的 admin 配置 GET/PUT 接口。
- [反馈编号：无] 四个配置写接口统一要求调用方传入 `X-Request-Id` 请求头，便于后端配置审计和问题追踪；本轮只做 API 封装，不开放页面编辑表单。
- [反馈编号：无] `scripts/check-admin-integration-ready.mjs` 新增“门店预约 admin 配置 API client”检查，确认四配置块读取/写入入口和 `X-Request-Id` 已存在。
- [反馈编号：无] TDD 验证：RED 阶段 `storeAppointments.test.ts` 失败于新 API client 函数缺失，`check-admin-integration-ready.test.mjs` 失败于 `storeAppointmentConfigApiClient` 缺失；GREEN 后对应定向测试 2 个文件、14 项通过。
- [反馈编号：无] 门店预约 admin 配置前端接入进入契约同步切片：将旧“admin API 缺口”只读区调整为“admin 配置契约”，四个中性配置面统一标记为 `backend-ready-frontend-pending`，表示后端契约已具备但前端编辑表单尚未接入。
- [反馈编号：无] `scripts/check-admin-integration-ready.mjs` 将 `adminApiGaps` 收敛为 `adminConfigContract`，同步输出后端正式接口、权限 `admin:store-appointment-config:manage`、候选可写字段和排除字段。
- [反馈编号：无] 修正预约规则配置接口路径为后端正式契约 `GET/PUT /api/admin/store-appointment-config/rules/{storeCode}`，不再沿用旧候选 `/stores/{storeCode}/rules`。
- [反馈编号：无] 本轮仍不开放配置保存、真实支付、会员、核销、客户资料、CRM、服务记录、员工账号、真实排班、消息通知、退款或客户账户策略。
- [反馈编号：无] TDD 验证：RED 阶段 `check-admin-integration-ready.test.mjs` 失败于 `adminConfigContract` 缺失，`StoreAppointmentsPage.test.ts` 失败于页面仍展示“admin API 缺口”；GREEN 后对应定向测试 2 个文件、16 项通过。
- [反馈编号：无] 门店预约 admin 新增 candidate 配置面的“admin API 缺口”只读清单，覆盖门店资料、项目目录、员工名册和预约规则 4 个候选配置面。
- [反馈编号：无] `scripts/check-admin-integration-ready.mjs` 同步输出 `adminApiGaps`：候选受控接口、所需权限 `admin:store-appointment-config:manage`、候选可写字段和必须排除字段；这些接口仍标记为 `missing-admin-api`，不作为已实现后端能力。
- [反馈编号：无] 页面清单明确排除 `tenantId`、`paymentAmount`、员工登录/权限、真实排班、通知模板、退款规则等生产字段，避免把 Demo 配置候选误认为真实配置编辑能力。
- [反馈编号：无] TDD 验证：RED 阶段 `check-admin-integration-ready.test.mjs` 失败于 `adminApiGaps` 缺失，`StoreAppointmentsPage.test.ts` 失败于页面缺少“admin API 缺口”；GREEN 后对应定向测试 2 个文件、16 项通过。
- [反馈编号：无] 门店预约 admin 新增“配置面 readiness”只读区，标注门店资料、项目目录、员工名册、预约规则、经营摘要、反馈跟进和服务记录 7 个配置面的 candidate / candidate-with-caution / blocked-by-production-design 状态。
- [反馈编号：无] `scripts/check-admin-integration-ready.mjs` 同步输出门店预约配置面 readiness 和 demo-only-excluded 清单，明确虚拟门店/员工、销售样板话术、模拟支付/核销/会员、wx storage 演示 key、病历/诊断类表达不进入 admin/模板默认配置。
- [反馈编号：无] 本轮仍不新增配置写入、真实支付、真实会员、真实核销、客户资料、CRM 跟进或服务记录，只为后续 admin/模板设计固定边界。
- [反馈编号：无] TDD 验证：RED 阶段 `check-admin-integration-ready.test.mjs` 失败于 `configSurfaces` 缺失，`StoreAppointmentsPage.test.ts` 失败于页面缺少“配置面 readiness”；GREEN 后对应定向测试 2 个文件、14 项通过。
- [反馈编号：无] 门店预约 admin 工作台新增“配置快照”只读区，调用 `GET /api/miniapps/{appCode}/stores/{storeCode}/booking-config` 核对启用门店的门店资料、服务项目、员工名册、员工项目关系和基础预约规则。
- [反馈编号：无] `src/api/storeAppointments.ts` 新增 `getStoreAppointmentBookingConfig` 及中性配置响应类型；字段沿用后端 `priceText`、外部 code 和基础规则，不引入生产支付金额、会员、核销、客户资料、CRM 跟进或服务记录模型。
- [反馈编号：无] 配置快照页面明确“只读展示，不保存配置”，仅作为 admin 接入前置核对能力；暂不新增门店资料、项目目录、员工名册、预约规则的后台写入入口。
- [反馈编号：无] TDD 验证：RED 阶段 `storeAppointments.test.ts` 失败于 `getStoreAppointmentBookingConfig is not a function`，`StoreAppointmentsPage.test.ts` 失败于配置快照入口未调用；GREEN 后对应定向测试 2 个文件、11 项通过。
- [反馈编号：无] 门店预约 admin 工作台新增受控状态流转：具备 `admin:store-appointment:manage` 权限时，可按后端状态机执行 `pending -> confirmed`、`confirmed -> arrived`、`arrived -> completed`、`pending/confirmed -> cancelled`。
- [反馈编号：无] `src/api/storeAppointments.ts` 新增 `updateStoreAppointmentStatus`，仅封装 `POST /api/admin/store-appointments/{appointmentId}/status` 的 `status` 字段，不引入支付、会员、核销、客户资料或服务记录。
- [反馈编号：无] 页面状态操作完成后刷新当前详情和预约列表；无 `manage` 权限时隐藏全部状态按钮，状态动作仍不包含退款、通知、核销或服务备注。
- [反馈编号：无] TDD 验证：RED 阶段 `storeAppointments.test.ts` 失败于 `updateStoreAppointmentStatus` 缺失，`StoreAppointmentsPage.test.ts` 失败于状态动作缺失；GREEN 后对应定向测试通过。
- [反馈编号：无] 新增门店预约 admin 首片：`/store-appointments` 只读预约工作台，支持按门店、项目、员工、预约日和状态筛选，展示预约列表、详情抽屉和状态日志。
- [反馈编号：无] 新增 `src/api/storeAppointments.ts`，封装 `POST /api/admin/store-appointments/page` 和 `GET /api/admin/store-appointments/{appointmentId}` 两个只读接口；首片暂不封装状态流转接口。
- [反馈编号：无] 主菜单新增全局“门店预约”入口，权限码为 `admin:store-appointment:view`；`scripts/check-admin-integration-ready.mjs` 同步检查门店预约路由、API 模块和页面模块。
- [反馈编号：无] TDD 验证：RED 阶段 `storeAppointments.test.ts` 失败于 API 模块缺失，`router.test.ts` / `check-admin-integration-ready.test.mjs` 失败于路由和模块检查缺失，`StoreAppointmentsPage.test.ts` 失败于占位页未加载分页和详情；GREEN 后对应定向测试均通过。
- [反馈编号：无] 新增 `docs/store-appointment-admin-precheck.md`，收口门店预约 admin 接入前置判断：复用现有 API client、路由、菜单、详情抽屉和权限模式，首片建议限定为只读列表 + 详情抽屉。
- [反馈编号：无] `scripts/check-admin-integration-ready.mjs` 增加门店预约 admin 前置预检，输出后端接口、筛选字段、状态、权限、首片策略和排除能力，明确真实支付、会员、核销、客户资料、CRM 跟进和服务记录不进入首片。
- [反馈编号：无] TDD 验证：RED 阶段 `scripts/check-admin-integration-ready.test.mjs` 失败于 `report.storeAppointment` 缺失；GREEN 后同测试文件 4 项通过。

## 2026-06-18

- [反馈编号：无] 失信/限高查询详情抽屉新增 crawler 分组结果的结构化预览：展示结果总览、主体记录、风险标签、记录字段和前 5 条案件摘要，同时保留原始结果预览作为兜底。
- [反馈编号：无] TDD 验证：先补充 `LegalCreditQueriesPage.test.ts` 对 `groups[].records[].cases[]` 结果结构的断言，RED 阶段失败于旧页面仅展示原始 JSON；GREEN 后同测试文件 12 项通过。
- [反馈编号：无] 失信/限高查询列表补充操作口径说明：明确认证律师不等于可信名单，未进入可信名单的查询需后台审核，并说明“重新查询”仅用于失败、取消、待复核或已发布任务再次进入查询队列。
- [反馈编号：无] 失信/限高查询详情抽屉补充“发起用户”字段，优先展示 `lma-xxxxxxxx` 用户编号；结果摘要在详情中优先使用后端结果对象摘要，避免列表旧摘要覆盖最新查询结果。
- [反馈编号：无] 失信/限高查询详情的空结构化结果从孤立 `-` 调整为“暂无结构化结果”，便于运营区分“无结果 JSON”和页面异常。
- [反馈编号：无] TDD 验证：先补充 `LegalCreditQueriesPage.test.ts` 对详情发起用户和空结果提示的断言；修复后同测试文件 10 项通过。
- [反馈编号：无] 失信/限高查询任务列表新增“发起用户”列，优先展示用户端编号 `lma-xxxxxxxx`，便于后台直接识别查询发起人。
- [反馈编号：无] 生成记录页“查看用户”改为当前页打开用户详情右侧抽屉，不再跳转到用户管理页；用户管理、生成记录和法律表单事件的用户详情身份表统一展示“用户编号”。
- [反馈编号：无] TDD 验证：RED 阶段覆盖失信/限高列表缺少发起用户、生成记录查看用户仍跳转、用户详情缺少用户编号；GREEN 后相关 4 个页面测试 31 项通过。
- [反馈编号：无] 已将失信/限高白名单与任务操作优化部署到生产 admin 静态资源：本地 `npm.cmd run build` 通过后同步到 `/data/cekaitech-admin`，线上资源包含 `MiniappAccessListPage-CMgx3iZE.js`、`LegalCreditQueriesPage-Ohc9j5FI.js` 和 `miniappAccessList-BVq9QFRQ.js`；`admin.cekaitech.cn` Basic Auth 未授权返回 401。
- [反馈编号：无] 小程序名单管理页将“导入已通过律师”改为“选择导入律师”：先读取已认证但未加入当前能力白名单的候选列表，再由管理员勾选导入，避免认证律师被全量加入可信名单。
- [反馈编号：无] 失信/限高查询任务页将“重新进入队列”改为“重新查询”，并仅在失败、过期、取消、待复核或已发布等需要重新拉取结果的状态展示，避免 queued/running/pending_review 阶段误操作。
- [反馈编号：无] 新增“小程序名单管理”页面 `/miniapp-access-list`，按小程序和能力维护可信名单与拒绝名单；首版对接阳律通 `legal_credit_query` 能力，支持分页筛选、手工新增、停用和从已通过律师认证批量导入可信名单。
- [反馈编号：无] 失信/限高查询任务页新增“加入可信并入队”快捷操作：待审核任务具备 `userId/identityId` 时，可先写入 `allow` 名单，再将本次任务审核通过进入 crawler 队列。
- [反馈编号：无] 新增 `src/api/miniappAccessList.ts` 受控 API 封装，admin 继续只通过 `miniapp-backend` 接口管理名单，不直连数据库。
- [反馈编号：无] TDD 验证：RED 阶段 `miniappAccessList.test.ts` 失败于 API 模块不存在、`MiniappAccessListPage.test.ts` 失败于页面不存在、`router.test.ts` 失败于菜单/路由缺失、`LegalCreditQueriesPage.test.ts` 失败于快捷动作未调用名单接口；GREEN 后对应定向测试均通过。
- [反馈编号：无] 修复失信/限高查询详情结果预览兼容性：除顶层 `records` 外，支持识别 crawler 常见 `items/list/results/data` 数组结构并优先展示首条记录，避免详情抽屉出现结果为空或只能看全量原始 JSON 的情况。
- [反馈编号：无] 优化失信/限高查询任务流转操作语义：待审核任务操作改为“通过并入队 / 驳回查询”，“重排队”改为“重新进入队列”，对应后台操作原因同步调整为更清晰的业务描述。
- [反馈编号：无] TDD 验证：先补充 `LegalCreditQueriesPage.test.ts` 对 crawler `items` 结果预览、入队/驳回/重新进入队列文案和调用参数的断言；修复后该测试文件 8 项通过。

## 2026-06-17

- [反馈编号：无] 修复服务请求列表业务状态展示优先级：当服务请求本身已处理、已关闭或已取消时，不再被已支付订单状态覆盖，避免 id=5 这类已保存成功的记录仍显示“已支付待服务”造成误判。
- [反馈编号：无] 法律表单事件页“查看用户”从跳转用户管理改为当前页直接打开用户详情右侧抽屉，减少二次点击；用户详情抽屉展示基础信息、小程序身份和手机号记录。
- [反馈编号：无] 统一拓宽重点详情抽屉：服务请求详情调整为 `760px`，用户详情与法律表单事件内嵌用户详情调整为 `720px`，提升身份、手机号、订单和退款等多列信息可读性。
- [反馈编号：无] 验证记录：`npm.cmd run test -- --run src/pages/legal-service-requests/LegalServiceRequestsPage.test.ts src/pages/legal-form-events/LegalFormEventsPage.test.ts src/pages/users/UsersPage.test.ts` 通过 3 个测试文件、38 项。

- [反馈编号：无] 修复法律工作区菜单“失信限高查询”入口落到通用占位页的问题：当后端菜单返回 `credit-restriction-queries` 时，admin 直接打开真实复核页 `/legal-credit-query-tasks`；没有真实页面的工作区菜单仍保留通用占位页。
- [反馈编号：无] 生产库只读核查确认当前 `legal_credit_query_task` 共 2 条，均为 `published`，暂无 `pending_review` 待审核任务；本轮仅修复后台入口可达性，不修改生产数据。
- [反馈编号：无] 验证记录：`npm.cmd run test -- --run src/layouts/AdminLayout.test.ts src/router/router.test.ts` 通过 2 个测试文件、25 项。
- [反馈编号：无] 工作区菜单入口修复已发布到 `admin.cekaitech.cn`，上线资源包含 `AdminLayout-BsH32udO.js` 和 `LegalCreditQueriesPage-Dp5LYkSe.js`；线上 `admin.cekaitech.cn` 未携带 Basic Auth 返回 401，公网 backend `/api/health` 返回 `UP`。

- [反馈编号：无] 律师认证审核页已发布到 `admin.cekaitech.cn`：`scripts/deploy-admin-static.ps1` 完成生产构建并同步到 `/data/cekaitech-admin/`，上线资源包含 `LawyerAuditsPage-BJalHsRs.js` 和 `LawyerAuditsPage-BuFdL-bM.css`。
- [反馈编号：无] 发布后校验：服务器 active dist 中确认包含 `/api/admin/user-audits/page`、`/approve`、`/reject` 调用；`https://admin.cekaitech.cn/` 未携带 Basic Auth 返回 401，后台律师审核分页接口未登录返回 401。
- [反馈编号：无] 律师认证审核页从占位升级为真实审核工作台：`/lawyer-audits` 支持按关键词、状态、审核类型和小程序筛选认证记录，展示用户编号、姓名、脱敏手机号、证号/说明、审核状态、提交和审核时间。
- [反馈编号：无] 新增 `src/api/lawyerAudits.ts`，对接 `miniapp-backend` 的 `/api/admin/user-audits/**` 受控接口；页面按 `admin:lawyer-audit:view` 与 `admin:lawyer-audit:manage` 区分查看和审核操作。
- [反馈编号：无] TDD 验证：RED 阶段 `LawyerAuditsPage.test.ts` 失败于占位页未调用接口且无记录/操作；GREEN 后 `npm.cmd run test -- src/api/lawyerAudits.test.ts src/pages/lawyer-audits/LawyerAuditsPage.test.ts` 通过 2 个测试文件、6 项。
- [反馈编号：无] 失信/限高查询任务复核页已发布到 `admin.cekaitech.cn`，`scripts/deploy-admin-static.ps1` 完成生产构建并同步到 `/data/cekaitech-admin/`，上线资源包含 `LegalCreditQueriesPage-DlTcmrzJ.js` 和 `LegalCreditQueriesPage-CSj8nWYQ.css`。
- [反馈编号：无] 发布后校验：服务器 active dist 中确认包含 `/api/admin/legal/credit-query-tasks/**` 调用；`https://admin.cekaitech.cn/` 未携带 Basic Auth 返回 401，后台失信/限高任务分页接口未登录返回 401。
- [反馈编号：无] 新增失信/限高查询任务 admin API client 首片：封装分页、详情、后台创建、取消、重排队、发布和敏感信息查看接口，路径统一对接 `miniapp-backend` 的 `/api/admin/legal/credit-query-tasks/**` 受控 API。
- [反馈编号：无] 新增 `/legal-credit-query-tasks` 后台路由和状态映射工具，权限码为 `admin:legal-credit-query:view`；本轮仅完成 API/route 骨架和占位页面，列表详情和操作页面在下一切片实现。
- [反馈编号：无] admin 仍不直连生产数据库、不控制本地 crawler 进程、不保存 worker token；失信/限高查询结果由 backend worker API 与后续复核页面承接。
- [反馈编号：无] TDD 验证：RED 阶段 `legalCreditQueries.test.ts` 和 `legalCreditQueryStatus.test.ts` 失败于模块不存在，`router.test.ts` 失败于 `/legal-credit-query-tasks` 路由缺失；GREEN 后 `npm.cmd test -- legalCreditQueries.test.ts legalCreditQueryStatus.test.ts router.test.ts` 通过 3 个测试文件、28 项。
- [反馈编号：无] 收口验证：`npm.cmd run quality` 通过，Vitest 37 个测试文件、191 项测试通过，`vue-tsc --noEmit` 和 `vite build` 通过；构建保留既有 `@vueuse/core` PURE 注释 warning 和 chunk size warning。
- [反馈编号：无] 失信/限高查询任务页从占位升级为复核工作台：支持任务分页、关键词/主体类型/状态/小程序筛选、详情抽屉、结果 JSON 预览、操作日志展示、发布结果、取消、重排队和敏感信息显式查看。
- [反馈编号：无] 页面权限按 `admin:legal-credit-query:view` 与 `admin:legal-credit-query:manage` 区分：有 view 权限可查看列表和详情，有 manage 权限才展示发布、取消、重排队操作；敏感信息只在点击“查看敏感信息”后调用后端 `sensitive-view` 接口。
- [反馈编号：无] TDD 验证：RED 阶段 `LegalCreditQueriesPage.test.ts` 失败于占位页未加载任务和操作；GREEN 后同测试文件 6 项通过。
- [反馈编号：无] 收口验证：`npm.cmd run quality` 通过，Vitest 38 个测试文件、197 项测试通过，`vue-tsc --noEmit` 和 `vite build` 通过；构建保留既有 `@vueuse/core` PURE 注释 warning 和 chunk size warning。

## 2026-06-16

- [反馈编号：无] 年度覆盖矩阵生产复核：服务器 active 静态资源 `DataGovernancePage-qqL96yll.js` 包含 `annual-common-data/coverage-matrix` 接口调用和“年度覆盖矩阵”页签；`https://admin.cekaitech.cn/` 未携带 Basic Auth 返回 `401`，`WWW-Authenticate=Basic realm="Cekai Admin"`。
- [反馈编号：无] 页面级登录后视觉复核受本地缺少 Basic Auth 凭据限制，未自动进入浏览器后台页面；本轮已完成可自动验证边界，包括静态资源内容、Basic Auth 保护和 backend 年度数据生产状态。后续可由人工登录 admin 后复核“数据同步/发布 -> 年度覆盖矩阵”是否展示 2023/2024 完整、2025 部分覆盖及平均工资缺口。
- [反馈编号：无] 年度覆盖矩阵增强治理提示：缺失指标从原始字段名改为中文业务名，并补充“缺口原因”“治理建议”“来源版本”“最近核验”列，便于区分 2024 当前有效数据与 2025 暂缓导入缺口。
- [反馈编号：无] TDD 验证：先补充 `DataGovernancePage.test.ts` 对中文缺失指标、缺口原因、治理建议、来源版本和最近核验日期的断言；实现后定向 `npm.cmd run test -- --run src/pages/data-governance/DataGovernancePage.test.ts src/api/dataGovernance.test.ts` 通过 2 个测试文件、15 项。
- [反馈编号：无] 数据同步/发布页新增“年度覆盖矩阵”页签，调用 `miniapp-backend` 受控只读接口 `POST /api/admin/data-governance/annual-common-data/coverage-matrix`，展示年度、省级地区覆盖、指标覆盖、缺失指标、缺失地区数量和最近同步批次。
- [反馈编号：无] `src/api/dataGovernance.ts` 新增年度数据覆盖矩阵类型和 API 封装，admin 继续只通过后端 API 读取治理结果，不直连数据库。
- [反馈编号：无] TDD 验证：RED 阶段 `src/api/dataGovernance.test.ts` 失败于 `getAnnualCommonDataCoverageMatrix is not a function`，`src/pages/data-governance/DataGovernancePage.test.ts` 失败于页面未调用年度覆盖矩阵接口；GREEN 后定向 `npm.cmd run test -- --run src/api/dataGovernance.test.ts src/pages/data-governance/DataGovernancePage.test.ts` 通过 2 个测试文件、15 项。
- [反馈编号：无] 收口验证：`npm.cmd run quality` 通过，Vitest 35 个测试文件、184 项测试通过，`vue-tsc --noEmit` 和 `vite build` 通过；构建保留既有 `@vueuse/core` PURE 注释 warning 和 chunk size warning。

## 2026-06-15

- [反馈编号：无] 新增全局“订单与退款”运营页 `/order-operations`，用于跨小程序查看统一订单、退款申请、退款通知和退款同步异常；页面继续只调用 `miniapp-backend` 受控 API，不直连数据库。
- [反馈编号：无] `src/api/adminOrders.ts` 扩展后台订单分页/汇总/主动查单、退款汇总、退款通知分页/汇总和退款同步异常看板封装，支撑订单运营页统一排障。
- [反馈编号：无] TDD 验证：RED 阶段 `router.test.ts` 失败于缺少订单运营菜单/路由，`adminOrders.test.ts` 失败于缺少订单/通知/异常 API 封装，`OrderOperationsPage.test.ts` 失败于页面未加载订单和退款运营数据；GREEN 后相关 3 个测试文件 27 项通过。
- [反馈编号：无] “订单与退款”运营页已发布到 `admin.cekaitech.cn`，`scripts\deploy-admin-static.ps1` 完成生产构建并同步到 `/data/cekaitech-admin/`，上线资源包含 `OrderOperationsPage-CIlMXRzE.js`；线上静态资源包含“订单与退款”，未认证访问仍返回 401 Basic Auth。补充有效后台应用 token 后完成浏览器数据页 smoke：订单、退款、退款通知和同步异常接口均返回 200，控制台无错误。
- [反馈编号：无] 服务请求详情抽屉右上角新增“刷新”按钮，可重新拉取服务请求详情、订单退款记录和外层列表，便于支付或退款回调后快速确认最新业务状态。
- [反馈编号：无] 退款创建、审核、发起和同步操作成功后改为刷新完整详情，避免只更新退款列表导致订单状态、业务状态或剩余退款语义滞后。
- [反馈编号：无] TDD 验证：RED 阶段 `LegalServiceRequestsPage.test.ts` 失败于详情页缺少“刷新”操作、退款同步后详情状态未更新；GREEN 后同测试文件 21 项通过。
- [反馈编号：无] 服务请求详情刷新交互已发布到 `admin.cekaitech.cn`，`scripts\deploy-admin-static.ps1` 完成生产构建并同步到 `/data/cekaitech-admin/`，上线资源包含 `LegalServiceRequestsPage-oV1z-yx9.js` 和 `LegalServiceRequestsPage-CWBmEXM9.css`；线上复核请求 `8` 详情中点击“刷新”后详情和列表接口均返回 200。
- [反馈编号：无] 服务请求详情“订单与退款”文案按订单状态动态展示：已退款订单显示“已完成退款，当前订单无剩余可退金额”，待支付订单提示不可退款，部分退款/已支付订单继续提示可创建退款申请。
- [反馈编号：无] TDD 验证：RED 阶段 `LegalServiceRequestsPage.test.ts` 失败于已退款订单仍展示“当前订单可在此创建退款申请并推进审核、发起、同步”；GREEN 后同测试文件 20 项通过。
- [反馈编号：无] 退款处理文案修复已发布到 `admin.cekaitech.cn`，上线资源包含 `LegalServiceRequestsPage-CWmvjZwR.js`；线上复核请求 `6` 详情已显示“已完成退款，当前订单无剩余可退金额。”且不再展示“创建退款申请”按钮。
- [反馈编号：无] 服务请求列表状态列从“处理状态”调整为“业务状态”：优先融合 `orderStatus/paymentStatus` 展示 `待支付/已支付待服务/部分退款/已退款`，避免已支付或已退款服务请求仍显示为“待处理”。
- [反馈编号：无] 服务请求详情新增独立“业务状态”展示，同时保留原“处理状态”，便于运营区分服务履约处理进度和支付退款事实状态。
- [反馈编号：无] TDD 验证：RED 阶段 `LegalServiceRequestsPage.test.ts` 失败于已支付/已退款记录仍显示“待处理”；GREEN 后同测试文件 19 项通过。
- [反馈编号：无] 业务状态语义修正已发布到 `admin.cekaitech.cn`，`scripts\deploy-admin-static.ps1` 完成生产构建并同步到 `/data/cekaitech-admin/`，上线资源包含 `LegalServiceRequestsPage-DAL64dLG.js` 和 `LegalServiceRequestsPage-DSHAgg2m.css`。
- [反馈编号：无] 服务请求支付报价与退款运营入口已发布到 `admin.cekaitech.cn`，`scripts\deploy-admin-static.ps1` 完成生产构建并同步到 `/data/cekaitech-admin/`，上线资源包含 `LegalServiceRequestsPage-Dasyl5-0.js` 和 `LegalServiceRequestsPage-BAGzzeWv.css`。
- [反馈编号：无] 发布后校验：`admin.cekaitech.cn` 未输入 Basic Auth 返回 401；服务器 active dist 中确认包含 `创建待支付订单`、`创建退款申请` 和 `用户编号` 文案，服务请求页已具备线上报价下单和退款首片入口。
- [反馈编号：无] 服务请求页支持展示后端返回的 `userCode`，表格和详情优先显示 `lma-xxxxxxxx` 用户编号，同时保留 `userId/identityId` 用于排障跳转。
- [反馈编号：无] 服务请求详情新增“订单与退款”区域：无订单且有管理权限时可直接填写金额、订单标题和内部备注，调用 `POST /api/admin/legal/service-requests/{requestId}/payment-order` 创建待支付订单。
- [反馈编号：无] 已有订单的服务请求详情展示订单号、订单金额、支付状态和订单状态；已支付订单可在详情中创建退款申请，并支持 `pending_review -> approved -> processing` 与单笔退款同步。
- [反馈编号：无] 新增 `src/api/adminOrders.ts`，封装退款分页、退款创建、退款状态更新和单笔同步接口，admin 继续只通过 `miniapp-backend` API 操作，不直连数据库。
- [反馈编号：无] TDD 验证：RED 阶段 `LegalServiceRequestsPage.test.ts` 失败于缺少用户编号、创建待支付订单和退款处理入口；GREEN 后 `npm.cmd test -- LegalServiceRequestsPage.test.ts legalServiceRequests.test.ts adminOrders.test.ts` 通过 25 项。

## 2026-06-13

- [反馈编号：无] 小程序编排 / 页面菜单管理支持通用页面编排节点：`miniapp_module` 显示为“通用页面模块”，`miniapp_feature` 显示为“通用功能入口”，可在既有编排编辑器中维护标题、说明、路径、动作、状态、排序和启用状态。
- [反馈编号：无] `miniapp_feature` 节点复用 `admin:miniapp-home-config:manage` 管理权限保存，admin 仍只通过 backend 编排接口读写，不直连数据库。
- [反馈编号：无] 补充页面测试覆盖编辑“服务请求”通用功能入口并保存 `sourceType=miniapp_feature` 的 payload。
- [反馈编号：无] 验证记录：`npm.cmd test -- MiniappOrchestrationPage.test.ts miniappOrchestration.test.ts` 通过 13 项；`npm.cmd run build` 通过，保留既有 Rollup 注释和 chunk size warning。
- [反馈编号：无] 页面菜单通用功能入口已发布到 `admin.cekaitech.cn`，`scripts\deploy-admin-static.ps1` 完成生产构建并同步 `/data/cekaitech-admin/`，上线资源包含 `MiniappOrchestrationPage-D-fbfYLo.js`。
- [反馈编号：无] 发布后校验：`admin.cekaitech.cn` Basic Auth 未登录返回 401，后台编排接口未登录返回 401；服务器 active dist 包含 `miniapp_feature` 和“通用功能入口”字样，确认页面菜单管理已具备通用入口编辑能力。

- [反馈编号：无] 文书入口配置事实源统一治理：admin 移除旧“文书目录配置”页面、路由、菜单、API 封装和页面测试，起诉文书入口统一在“小程序编排 / 页面菜单管理”维护。
- [反馈编号：无] 联调准备检查脚本移除旧 `/miniapp-document-catalog` 路由、`miniappDocumentCatalog` API 和页面模块检查项；“结果模板配置”页文案改为按页面菜单中的文书入口选择案件类型。
- [反馈编号：无] TDD 验证：RED 阶段 `router.test.ts` 和 `check-admin-integration-ready.test.mjs` 失败于旧入口仍在；GREEN 后 `npm.cmd run test -- --run src/router/router.test.ts scripts/check-admin-integration-ready.test.mjs` 通过 2 个测试文件、23 项。
- [反馈编号：无] 文书入口事实源治理已发布到 `admin.cekaitech.cn`，`scripts\deploy-admin-static.ps1` 完成生产构建并同步到 `/data/cekaitech-admin/`，上线资源包含 `index-BLKNh4tO.js`、`MiniappOrchestrationPage-D-oqcBke.js`、`PrivateLendingResultTemplatePage-BqnRZCH6.js`。
- [反馈编号：无] 发布后校验：active admin dist 中不再包含 `miniapp-document-catalog`、`miniappDocumentCatalog` 或“文书目录配置”旧入口字符串；`admin.cekaitech.cn` 未输入 Basic Auth 返回 401，`scripts\test-yanglvtong-experience-smoke.ps1` 通过。

- [反馈编号：无] 页面菜单管理默认展开策略调整为“展开到分组、折叠叶子列表”：进入菜单结构页时保留小程序、Tab 和模块层级可见，但默认收起直接包含功能叶子项的分组，降低长列表视觉疲劳。
- [反馈编号：无] 页面菜单树刷新后的选中节点匹配改为全树查找，避免默认折叠后因叶子节点不在可见扁平列表中导致选中态回退到根节点；搜索模式仍自动展示匹配路径和功能节点。
- [反馈编号：无] TDD 验证：RED 阶段 `MiniappOrchestrationPage.test.ts` 失败于默认仍展示 `诉讼费计算/LPR` 叶子项；GREEN 后同测试文件 10 项通过。
- [反馈编号：无] 结果模板配置页补齐劳动争议首片支持：劳动争议在案件类型选项中按可编辑模板展示，支持加载、编辑和预览后端 `labor` 结果模板。
- [反馈编号：无] 管理端预览样例新增劳动者、用人单位、用工日期、争议类型、主张金额和证据状态字段，避免切换劳动争议后仍使用民间借贷样例。
- [反馈编号：无] 验证记录：RED 阶段 `npm.cmd test -- PrivateLendingResultTemplatePage.test.ts` 失败于 labor 预览仍提交借贷样例；GREEN 后同命令通过 8 项；收口 `npm.cmd run quality` 通过 35 个测试文件、172 项并完成生产构建，`git diff --check` 无空白错误，仅 Windows 换行提示。
- [反馈编号：无] 劳动争议结果模板配置首片已发布到 `admin.cekaitech.cn`，`scripts/deploy-admin-static.ps1` 完成生产构建并同步到 `/data/cekaitech-admin/`，上线资源包含 `PrivateLendingResultTemplatePage-CGnkRVZa.js`。
- [反馈编号：无] 发布后校验：服务器静态资源确认包含 `employeeName/labor/劳动争议` 劳动争议样例字段；公网 smoke 通过，admin Basic Auth 未登录返回 401，backend 劳动争议 schema/generate 公开接口验证通过。
- [反馈编号：无] 劳动争议结果模板配置二期：`PrivateLendingResultTemplatePage` 的 `labor` 预览样例补齐月工资、欠薪期间/金额、解除原因、经济补偿或赔偿、二倍工资和仲裁状态字段。
- [反馈编号：无] 页面测试新增二期样例断言，确保后台预览劳动争议模板时向 backend 传入与 V138 schema 匹配的结构化数据；RED 阶段定向测试失败于缺少二期字段，GREEN 后同命令 8 项通过。
- [反馈编号：无] 劳动争议结果模板配置二期已发布到 `admin.cekaitech.cn`，`scripts/deploy-admin-static.ps1` 完成生产构建并同步到 `/data/cekaitech-admin/`，上线资源包含 `PrivateLendingResultTemplatePage-CoMBmt9Y.js`。
- [反馈编号：无] 发布后校验：服务器静态资源确认包含 `monthlyWage/arbitrationStatus` 二期样例字段；公网 smoke 通过，admin Basic Auth 未登录返回 401，backend V138 劳动争议 schema/generate 公开接口验证通过。

## 2026-06-12

- [反馈编号：无] 起诉文书结果模板预览排版已发布到 `admin.cekaitech.cn`，`scripts/deploy-admin-static.ps1` 完成生产构建并同步到 `/data/cekaitech-admin/`，上线资源包含 `PrivateLendingResultTemplatePage-Bhak0YDG.js` 和 `PrivateLendingResultTemplatePage-abQesw6r.css`。
- [反馈编号：无] 发布后校验：公网 smoke 通过，`admin.cekaitech.cn` 未输入 Basic Auth 返回 401，`api.cekaitech.cn/api/health` 返回 `UP`；服务器静态资源确认包含 `document-paper/document-line-signature/document-line-title` 文书排版类名，上一版静态产物已保留在 `/data/cekaitech-admin/.previous/`。
- [反馈编号：无] 结果模板配置页预览从纯 `<pre>` 文本升级为基础文书纸面预览：标题居中，小标题加粗，正文缩进，“此致/人民法院/具状人/日期”按文书习惯对齐展示；本轮仅调整 admin 预览渲染，不改后端生成内容、模板字段或接口契约。
- [反馈编号：无] TDD 验证：先补充结果模板页测试复现旧预览缺少 `.document-paper` 且仍使用 `<pre>`；GREEN 后 `npm.cmd run test -- --run src/pages/private-lending-result-template/PrivateLendingResultTemplatePage.test.ts` 通过 1 个测试文件、6 项。
- [反馈编号：无] 结果模板配置页支持“离婚纠纷”首片：当 backend options 返回 `divorce.templateSupported=true` 时，页面加载离婚模板编辑器并使用离婚样例数据预览，避免继续提交民间借贷样例字段。
- [反馈编号：无] TDD 验证：RED 阶段切换 `divorce` 后预览仍提交 `borrowerName/lenderName` 等民间借贷样例；GREEN 后 `npm.cmd run test -- --run src/api/privateLendingResultTemplate.test.ts src/pages/private-lending-result-template/PrivateLendingResultTemplatePage.test.ts` 通过 2 个测试文件、6 项；收口 `npm.cmd run quality` 通过 35 个测试文件、169 项并完成生产构建。
- [反馈编号：无] 离婚纠纷结果模板配置首片已发布到 `admin.cekaitech.cn`，`scripts/deploy-admin-static.ps1` 完成生产构建并同步到 `/data/cekaitech-admin/`，上线资源包含 `PrivateLendingResultTemplatePage-CfdkUiv5.js`。
- [反馈编号：无] 发布后校验：服务器静态资源确认包含 `divorce/plaintiffName` 离婚样例字段；公网 smoke 通过，admin Basic Auth 未登录返回 401，backend 离婚 schema/generate 公开接口验证通过。
- [反馈编号：无] 起诉文书生成多结果模板配置首片已发布到 `admin.cekaitech.cn`，`scripts/deploy-admin-static.ps1` 完成生产构建并同步到 `/data/cekaitech-admin/`，上线资源包含 `PrivateLendingResultTemplatePage-CKOHrQyy.js`。
- [反馈编号：无] 发布后校验：服务器静态资源中确认包含 `/api/admin/case-result-template` 和“结果模板配置”文案；公网 smoke 通过，admin Basic Auth 未登录返回 401，backend 新结果模板接口未登录返回 401。
- [反馈编号：无] 起诉文书生成“结果模板配置”从固定民间借贷页面升级为案件类型选择式页面，顶部展示文书目录中的 `民间借贷纠纷`、`离婚纠纷`、`劳动争议` 及生成配置状态。
- [反馈编号：无] 管理端 API 封装切换到 backend 通用 `/api/admin/case-result-template` 接口；当前仅 `private_lending` 可编辑、保存和预览，未具备生成配置的案件类型显示“暂无生成配置”并隐藏模板编辑器。
- [反馈编号：无] 菜单描述从“民间借贷结果模板和预览”调整为“起诉文书生成结果模板和预览”，避免 admin 入口与多模板模块定位不一致。
- [反馈编号：无] TDD 验证：RED 阶段 API 函数、案件类型选择和菜单文案测试失败；GREEN 后定向 `privateLendingResultTemplate` API、页面和 router 测试通过 26 项。
- [反馈编号：无] 小程序编排 / 页面菜单管理统一承载功能上线生命周期，功能编辑表单从“业务状态”调整为“生命周期”，状态选项收敛为 `published/pending_release/paused/retired`。
- [反馈编号：无] 页面菜单管理兼容旧接口返回的 `open/coming_soon/consultation/locked/disabled`，选中节点后自动归一为新生命周期并保存回 backend，降低生产迁移前后的短暂不一致风险。
- [反馈编号：无] 法律工具中心能力库去掉能力状态列、启用状态列和状态下拉操作；能力页只维护能力元数据，真正是否对小程序前台可见由页面菜单入口生命周期控制。
- [反馈编号：无] TDD 验证：页面菜单生命周期测试和能力页“不暴露发布生命周期操作”测试通过；`npm.cmd run quality` 通过 35 个测试文件、168 项测试，并完成生产构建。

## 2026-06-11

- [反馈编号：无] admin 静态资源已发布到 `admin.cekaitech.cn`，构建时注入 `VITE_API_BASE_URL=https://api.cekaitech.cn`，上线资源包含 `LegalToolCenterPage-BsUFdRjP.js` 和 `MiniappWorkbenchPage-VNEwOTtl.js`。
- [反馈编号：无] 发布后 smoke 通过：`scripts/test-yanglvtong-experience-smoke.ps1` 中 admin Basic Auth 返回 401、后台生产巡检接口未登录返回 401；公网 API health 返回 `UP`，确认新 admin 静态页面对接生产测试 API。
- [反馈编号：无] 法律工具中心能力列表新增“状态”下拉操作，支持将工具人工调整为已上线、待发布、阻塞、人工暂缓或已下架；状态调整调用 backend 窄接口，不再重提完整能力配置。
- [反馈编号：无] 状态流转默认写入明确内部备注：如“人工确认通过门禁后上线”“人工原因暂缓上线”“工具已下架，不进入前台和交付队列”，便于后续审计和队列说明。
- [反馈编号：无] TDD 验证：RED 阶段 API 测试失败于 `updateLegalToolCapabilityStatus is not a function`，页面测试失败于 `vm.updateCapabilityLifecycleStatus is not a function`；GREEN 后定向测试通过。
- [反馈编号：无] 小程序工作台接入法律工具生命周期队列口径：摘要卡片从“可启用候选/需复核”调整为“已上线/待发布队列/阻塞/人工暂缓”，与 backend `enabled/pending_release/blocked/paused/retired` 状态保持一致。
- [反馈编号：无] 法律工具中心能力状态选项更新为“已上线、待发布、阻塞、人工暂缓、已下架”，新增能力默认进入 `pending_release`，避免未验收工具误配置为前台可见。
- [反馈编号：无] 工作台列表新增生命周期列，`live` 显示“已上线”，`pass` 显示“待发布”，`deferred` 显示“已暂缓”，便于后续按队列逐个推进人工启用。
- [反馈编号：无] 新增“小程序工作台”菜单和页面，切换到阳律通工作区后进入该小程序自己的工作台，展示当前小程序的工具完整性摘要、可启用候选、需复核和阻塞工具列表；全局后台仍保持平台工作台，不展示小程序业务概览。
- [反馈编号：无] 工作区切换交互调整：从全局切换到业务小程序后自动进入 `/miniapp-workbench`，切回全局后进入 `/dashboard`，避免管理员误停留在上一个小程序的具体配置页。
- [反馈编号：无] `legalToolCenter` API 封装新增 `inspectLegalToolReadiness`，调用 backend `POST /api/admin/legal-tool-center/readiness/inspect`；工作台只读展示完整性检查结果，不启用工具、不下架工具、不修改生产数据。
- [反馈编号：无] TDD 验证：RED 阶段 `legalToolCenter.test.ts` 失败于 `inspectLegalToolReadiness is not a function`，`router.test.ts` 失败于缺少 `/miniapp-workbench` 菜单/路由，`MiniappWorkbenchPage.test.ts` 失败于页面文件不存在，`AdminLayout.test.ts` 失败于切换工作区未跳转；GREEN 后定向测试通过。
- [反馈编号：无] 数据同步/发布页新增 “预览 LPR JSON” 入口，调用 `POST /api/admin/data-governance/lpr-rates/preview` 展示新增、跳过、更新、冲突摘要和明细；预览不刷新同步批次，不提示已发布，不写生产数据。
- [反馈编号：无] `dataGovernance` API 封装新增 `previewLprRates`，LPR JSON 解析保留 `requestId/sourceKey/sourceVersion/sourceClient/collectedAt/lastCheckedDate/mode/payloadHash` 等治理字段，避免只上传 `items` 导致后端校验信息丢失。
- [反馈编号：无] TDD 验证：RED 阶段 `dataGovernance.test.ts` 失败于 `previewLprRates is not a function`，`DataGovernancePage.test.ts` 失败于页面丢失 LPR payload 元字段且缺少预览按钮；GREEN 后两个测试文件均通过。

## 2026-06-09

- [反馈编号：无] 数据同步/发布页“生产巡检”增强要素式示范文本摘要：示范文本卡片展示当前来源、来源版本、核验日期、`226 / 226` 数量进度和文件元数据完整度，便于 Docimax 226 份文本上线后直接在后台判断下载链路数据是否完整。
- [反馈编号：无] `ProductionStatus.elementTemplate` 类型补充 `sourceKey`、`sourceName`、`sourceVersion`、`lastCheckedDate`、`expectedCount`、`filePathCount` 和 `ready` 字段；页面对旧接口短暂未更新场景保留兜底展示，不直连数据库、不发起静态文件 HEAD。
- [反馈编号：无] TDD 验证：RED 阶段 `npm.cmd run test -- --run src/api/dataGovernance.test.ts src/pages/data-governance/DataGovernancePage.test.ts` 失败于页面仍展示旧“示范文本 226 份”摘要；GREEN 阶段同命令通过，2 个测试文件、12 项 Vitest。
- [反馈编号：无] 收口验证：`npm.cmd run quality` 通过，33 个测试文件、158 项 Vitest，`vue-tsc --noEmit` 和 `vite build` 通过；`git diff --check` 无空白错误，仅 Windows 换行提示。
- [反馈编号：无] 线上静态资源已通过 `scripts\deploy-admin-static.ps1` 同步到 `/data/cekaitech-admin/`，本次构建产物包含 `DataGovernancePage-Bnyahp8-.js`；脚本自动保留上一版 `/data/cekaitech-admin/.previous/`，公网 `https://admin.cekaitech.cn` 未带 Basic Auth 返回 401，验证 Basic Auth 仍生效。
- [反馈编号：无] 数据同步/发布页新增“生产巡检”只读摘要，调用 `POST /api/admin/data-governance/production-status` 展示 Flyway、品牌旧文案残留、LPR、年度数据、要素式示范文本文件元数据和民事案由节点状态，便于后台直接核对生产测试环境就绪情况。
- [反馈编号：无] 新增 `getProductionStatus` API 封装和页面测试覆盖；管理端只通过 `miniapp-backend` 读取巡检状态，不直连数据库、不触发 crawler、不写生产数据。
- [反馈编号：无] 管理后台登录页移除默认填充的 `admin` / `123456`，避免试运行环境误保留弱口令提示；新增登录页测试覆盖用户名和密码默认空值。
- [反馈编号：无] 管理后台登录后默认停留在“全局后台”，不再自动进入法律小程序工作区；平台级菜单与小程序业务菜单按工作区范围隔离，只有切换到具体小程序后才展示首页配置、法律工具中心、数据治理等业务菜单。
- [反馈编号：无] 小程序业务菜单增加工作区白名单：法律工具中心、首页配置、数据治理、文书目录等法律助手专属菜单只在“阳律通”工作区展示，切换到其他小程序且未配置对应业务菜单时不再复用法律助手菜单。
- [反馈编号：无] 管理后台工作区兜底名称同步为“阳律通”，与小程序当前对客展示名称一致；补充工作区加载、切换和菜单隔离测试。
- [反馈编号：无] 新增 `scripts/deploy-admin-static.ps1`，支持本地构建后通过 SSH/SCP 上传 `dist/` 到服务器临时目录，并在服务器侧用 `rsync --delete` 同步到 `/data/cekaitech-admin/`，提升 admin 静态前端发布效率；`docs/production-runbook.md` 同步记录发布和回滚路径。
- [反馈编号：无] 修复 admin 静态发布脚本生产构建环境：发布时默认注入 `VITE_API_BASE_URL=https://api.cekaitech.cn` 并校验构建产物包含生产 API 域名，避免线上登录请求误打到 `admin.cekaitech.cn/api` 静态域名。

## 2026-06-05

- [反馈编号：无] 补齐 `admin.cekaitech.cn` 试运行上线前必做项：`docs/production-runbook.md` 明确 Nginx Basic Auth、生产后台管理员默认密码修改、生产 API 域名 `https://api.cekaitech.cn`、平台后台与小程序工作区隔离的验证步骤。
- [反馈编号：无] 管理后台品牌和首页文案收敛为公司级平台后台：侧边栏展示“平台管理后台”，首页入口调整为“平台工作台”，并增加试运行上线前提醒。
- [反馈编号：无] 工作区隔离测试补充全局后台不加载具体小程序菜单的断言，避免将阳光法律助手等工作区菜单误展示到平台全局后台。
- [反馈编号：无] 收口验证：`npm.cmd run test -- --run src/stores/workspace.test.ts` 通过，1 个测试文件、5 项 Vitest；`npm.cmd run quality` 通过，32 个测试文件、152 项 Vitest，类型检查和生产构建通过；`git diff --check` 无空白错误，仅 Windows 换行提示。
- [反馈编号：无] 将本地演示运行日志目录 `.runtime-logs/` 加入 `.gitignore`，避免启动脚本生成的本地进程记录和日志干扰管理端工作区状态。
- [反馈编号：无] 法律工具中心新增“示范文本文件”管理页签，展示要素式示范文本静态文件域名、文件总数、缺失元数据、发布校验状态、文件清单和校验问题。
- [反馈编号：无] 新增要素式示范文本文件 API 封装：`manifestElementTemplateFiles`、`validateElementTemplateFiles`、`previewElementTemplateFileImport`、`applyElementTemplateFileImport`，分别对接后端清单、发布前校验、导入预检和确认导入接口。
- [反馈编号：无] 管理端支持粘贴结构化 JSON 批量导入示范文本文件元数据；确认导入需具备 `admin:legal-tool-center:manage` 权限，并继续只通过 `miniapp-backend` 受控 API 写入业务数据。
- [反馈编号：无] TDD 验证：RED 阶段 `LegalToolCenterPage.test.ts` 失败于页面未调用示范文本文件清单/校验接口；GREEN 阶段 `npm.cmd run test -- --run src/pages/legal-tool-center/LegalToolCenterPage.test.ts` 通过，1 个测试文件、13 项 Vitest。
- [反馈编号：无] 收口验证：`npm.cmd run quality` 通过，32 个测试文件、151 项 Vitest，类型检查和构建通过；`git diff --check` 无空白错误，仅 Windows 换行提示。

## 2026-06-03

- [反馈编号：无] 首页模块配置新增“展示更多”开关：`src/pages/miniapp-home-config/MiniappHomeConfigPage.vue` 在模块列表展示“展示更多/全量展示”，模块编辑弹窗支持维护 `showMoreEnabled`。
- [反馈编号：无] `src/api/miniappHomeConfig.ts` 模块类型和保存 payload 新增 `showMoreEnabled`，与后端 admin 首页模块接口保持一致；本片不新增任意 WXML/HTML/CSS/JS 下发能力。
- [反馈编号：无] TDD 验证：管理端页面测试先失败于缺少“全量展示”状态；实现后 `npm.cmd test -- --run src/pages/miniapp-home-config/MiniappHomeConfigPage.test.ts src/api/miniappHomeConfig.test.ts` 通过 7 项。

## 2026-06-02

- [反馈编号：LMA-FB-044] 法律工具中心测试样本新增 `lawyer_fee_reference` 能力、曝光入口和 `lawyer_fee_reference_sample_2026` 来源记录，覆盖“律师费参考”标题、`scale` 图标、`/pages/lawyer-fee-reference/lawyer-fee-reference` 跳转、`miniapp_local_calculation` 执行方式、`high` 风险等级和 `lawyer-fee-reference-sample-2026` 来源版本；本阶段不新增独立律师费规则维护页。
- [反馈编号：LMA-FB-044] TDD 验证：RED 阶段管理端法律工具中心样本缺少律师费参考能力；GREEN 阶段 `npm.cmd run test -- --run src/pages/legal-tool-center/LegalToolCenterPage.test.ts` 通过，1 个测试文件、12 项 Vitest。
- [反馈编号：LMA-FB-044] 收口验证：`npm.cmd run quality` 通过，32 个测试文件、149 项 Vitest，类型检查和构建通过；`git diff --check` 无空白错误，仅 Windows 换行提示。

- [反馈编号：LMA-FB-043] 法律工具中心测试样本新增 `arbitration_fee` 能力、曝光入口和 `state_council_1995_arbitration_fee` 来源记录，覆盖“仲裁费用参考”标题、`scale` 图标、`/pages/arbitration-fee/arbitration-fee` 跳转、`miniapp_local_calculation` 执行方式、`high` 风险等级和 `state-council-1995-arbitration-fee` 来源版本；本阶段不新增独立规则维护页。
- [反馈编号：LMA-FB-043] TDD 验证：RED 阶段管理端法律工具中心样本缺少仲裁费用能力；GREEN 阶段 `npm.cmd run test -- --run src/pages/legal-tool-center/LegalToolCenterPage.test.ts` 通过，1 个测试文件、12 项 Vitest。
- [反馈编号：LMA-FB-043] 收口验证：`npm.cmd run quality` 通过，32 个测试文件、149 项 Vitest，类型检查和构建通过；`git diff --check` 无空白错误，仅 Windows 换行提示。
- [反馈编号：LMA-FB-043] 提交 `e8afd46 test: cover arbitration fee legal tool` 已推送 GitHub，并配合小程序总台账回写企业微信智能表格 `oRvzFj`。

- [反馈编号：LMA-FB-041] 数据同步/发布页新增年度常用数据治理能力：展示年度数据同步批次、年度数据修订记录，并支持粘贴规范化年度数据 JSON 通过 `miniapp-backend` 受控 API 导入。
- [反馈编号：LMA-FB-041] `src/api/dataGovernance.ts` 新增 `pageAnnualCommonDataSyncBatches`、`pageAnnualCommonDataRevisions`、`syncAnnualCommonData` 封装，分别调用 `/api/admin/data-governance/annual-common-data-sync-batches/page`、`/annual-common-data-revisions/page`、`/annual-common-data/sync`。
- [反馈编号：LMA-FB-041] 管理端继续不直连数据库、不控制 crawler 进程；年度数据导入只接收结构化 JSON，并交由后端做幂等、冲突和 revision 审计。
- [反馈编号：LMA-FB-041] TDD 验证：RED 阶段 `npm.cmd run test -- --run src/api/dataGovernance.test.ts src/pages/data-governance/DataGovernancePage.test.ts` 失败于年度 API 函数和页面 JSON 编辑器缺失；GREEN 阶段同命令通过，2 个测试文件、11 项 Vitest。

- [反馈编号：LMA-FB-037] 法律工具中心新增“常用年度数据”维护 tab，支持查看和保存地区、年度、指标、数值、来源版本、核验日期、状态等结构化年度数据。
- [反馈编号：LMA-FB-037] 新增 `pageAnnualCommonData`、`saveAnnualCommonData` API 封装和页面/API 测试覆盖；`vite.config.ts` 排除 `.worktrees/**`，避免旧 worktree 测试被 Vitest 重复扫描。
- [反馈编号：LMA-FB-037] 收口验证：`npm.cmd run quality` 通过，32 个测试文件、144 项 Vitest，类型检查和构建通过；`git diff --check` 无空白错误，仅 Windows 换行提示。

- [反馈编号：LMA-FB-035] 法律工具中心诉讼费用规则测试样本新增 `execution_application_fee`、`preservation_application_fee`、`payment_order_application_fee` 三类申请费用规则，覆盖“申请执行费”“申请保全费”“申请支付令费用”在既有诉讼费用规则表内展示；管理端继续复用既有规则维护页，不新增独立页面。
- [反馈编号：LMA-FB-035] 诉讼费用规则摘要兼容仅配置费用上限或下限的分段，避免申请保全费等封顶规则在列表摘要中展示 `null-5000`；补充申请保全费 2000000 元预览返回 5000 元封顶结果的页面测试。
- [反馈编号：LMA-FB-035] TDD 验证：RED 阶段 `npm.cmd test -- LegalToolCenterPage.test.ts` 失败于页面缺少“费用上限 5000”摘要；GREEN 阶段同命令通过，1 个测试文件、11 项 Vitest。

- [反馈编号：LMA-FB-034] 法律工具中心测试样本新增 `small_claim_limit` 能力、曝光入口和 `civil_procedure_law_small_claim_limit` 官方来源记录，覆盖“小额诉讼限额核对参考”标题、`badge-check` 图标、`/pages/small-claim-limit/small-claim-limit` 跳转、`miniapp_local_calculation` 执行方式、`medium` 风险等级、`static_rule` 数据依赖和 `civil-procedure-law-small-claim-limit` 来源版本；本阶段管理端仅补法律工具中心展示测试，不新增年度数据维护页。
- [反馈编号：LMA-FB-034] TDD 验证：RED 阶段 `npm.cmd test -- LegalToolCenterPage.test.ts` 失败于能力 fixture 缺少 `small_claim_limit`；GREEN 阶段同命令通过，1 个测试文件、10 项 Vitest。

## 2026-06-01

- [反馈编号：LMA-FB-033] 法律工具中心测试样本新增 `work_injury_compensation` 能力、曝光入口和 `state_council_2010_work_injury_insurance_regulation` 官方来源记录，覆盖“工伤赔偿伤残等级参考”标题、`briefcase-medical` 图标、`/pages/work-injury-compensation/work-injury-compensation` 跳转、`miniapp_local_calculation` 执行方式、`high` 风险等级和 `state-council-2010-work-injury-insurance-regulation` 来源版本；本阶段不新增独立地区数据或规则维护页。
- [反馈编号：LMA-FB-033] TDD 验证：RED 阶段 `npm.cmd test -- LegalToolCenterPage.test.ts` 失败于页面缺少“工伤赔偿伤残等级参考”；GREEN 阶段同命令通过，1 个测试文件、10 项 Vitest。
- [反馈编号：LMA-FB-033] 复核修复：工伤赔偿管理端覆盖补充能力、曝光入口和来源 mock fixture 的结构化字段归属断言，避免只依赖整页文本中的通用风险等级和执行方式。
- [反馈编号：LMA-FB-033] 收口验证：`npm.cmd run quality` 通过，32 个测试文件、141 项 Vitest，`vue-tsc --noEmit` 和 `vite build` 通过；`git diff --check` 无空白错误，仅提示 Windows 换行转换。

- [反馈编号：LMA-FB-032] 法律工具中心测试样本新增 `traffic_personal_injury_compensation` 能力、曝光入口和 `spc_2022_personal_injury_compensation` 官方来源记录，覆盖“交通事故等人身损害赔偿参考”标题、`ambulance` 图标、`/pages/traffic-personal-injury-compensation/traffic-personal-injury-compensation` 跳转、`miniapp_local_calculation` 执行方式、`high` 风险等级和 `spc-2022-personal-injury-compensation` 来源版本；本阶段不新增独立规则维护页。
- [反馈编号：LMA-FB-032] TDD 验证：RED 阶段 `npm.cmd test -- LegalToolCenterPage.test.ts` 失败于页面缺少“交通事故等人身损害赔偿参考”；GREEN 阶段同命令通过，1 个测试文件、10 项 Vitest。
- [反馈编号：LMA-FB-032] 收口验证：`npm.cmd run quality` 通过，32 个测试文件、141 项 Vitest，`vue-tsc --noEmit` 和 `vite build` 通过；`git diff --check` 无空白错误，仅提示 Windows 换行转换。

- [反馈编号：LMA-FB-031] 法律工具中心测试样本新增 `bankruptcy_administrator_remuneration` 能力、曝光入口和 `spc_2007_bankruptcy_administrator_remuneration` 官方来源记录，覆盖“破产管理人报酬上限”标题、`scale` 图标、`/pages/bankruptcy-administrator-remuneration/bankruptcy-administrator-remuneration` 跳转、`miniapp_local_calculation` 执行方式、`high` 风险等级和 `spc-2007-bankruptcy-administrator-remuneration` 来源版本；本阶段不新增独立规则维护页。
- [反馈编号：LMA-FB-031] 收口验证：`npm.cmd run quality` 通过，32 个测试文件、141 项 Vitest，`vue-tsc --noEmit` 和 `vite build` 通过；`git diff --check` 无空白错误，仅提示 Windows 换行转换。

- [反馈编号：LMA-FB-030] 法律工具中心测试样本新增 `statutory_retirement_age` 能力、曝光入口和 `npc_2024_statutory_retirement_age` 官方来源记录，覆盖“延迟退休年龄”标题、`hourglass` 图标、`/pages/statutory-retirement-age/statutory-retirement-age` 跳转、`miniapp_local_calculation` 执行方式、`medium` 风险等级和 `npc-2024-statutory-retirement-age` 来源版本；本阶段不新增独立规则维护页。
- [反馈编号：LMA-FB-030] 定向验证：RED 阶段 `npm.cmd test -- LegalToolCenterPage.test.ts` 失败于页面缺少“延迟退休年龄”；GREEN 阶段同命令通过，1 个测试文件、10 项 Vitest。
- [反馈编号：LMA-FB-030] 收口验证：`npm.cmd run quality` 通过，32 个测试文件、141 项 Vitest，`vue-tsc --noEmit` 和 `vite build` 通过；仅保留既有 Rollup PURE 注释和 chunk size warning。本条为历史阶段记录，后续状态以对应反馈台账终态为准。

- [反馈编号：无] 小程序首页模块配置新增“展示数量”字段，列表展示 `visibleLimit`，编辑模块时可在 1-20 范围内配置；法律助手工具类可配置为 8，实现首页 2 行后自动由小程序显示“更多”入口。
- [反馈编号：无] 共享小程序图标选择器扩展为 50+ 受控开源语义图标，并新增搜索框，可按中文名称、场景词和 icon key 过滤；首页配置、法律工具中心和页面菜单管理共用同一选择器，提升其他小程序后续配置 ico 的复用一致性。
- [反馈编号：无] 图标库补充 `gavel`、`scroll-text`、`receipt-text`、`triangle-alert` 等法律/风险/材料场景 key，保留后台只写入结构化 `iconKey` 的模式，小程序端继续从本地主包白名单 SVG 渲染，不引入任意外链图标。
- [反馈编号：无] 页面菜单管理移除“近期法律工具 / codex_test_entry_2159”作为正常测试样例和搜索提示，改用“诉讼费计算”等真实业务功能验证树内搜索、选择、编辑和权限只读行为，避免内部联调入口被误认为对客功能。
- [反馈编号：无] 页面菜单管理新增树内搜索框，可按功能名称、路径、key 或来源定位节点；搜索时自动展示匹配节点的父级路径，方便运营找到“诉讼费计算”等位于长列表中的功能入口。
- [反馈编号：无] 页面菜单管理适配 `profile_local_feature` 账号页本地功能节点，“我的”Tab 下可查看 `我的审核`、`我的生成记录`、`我的收藏`、`用户协议与免责声明`，并显示只读说明，避免误以为这类本地页面已可在后台直接保存。
- [反馈编号：无] 小程序配置中心默认按对客可见口径加载页面编排树，请求 `POST /api/admin/miniapp-orchestration/tree` 时显式携带 `includeDisabled=false`，避免停用的历史联调模块默认出现在后台配置树中。
- [反馈编号：无] 小程序配置中心新增“显示停用项”恢复视图开关，开启后以 `includeDisabled=true` 拉取停用节点，并在树节点显示“已停用”标记，便于运营排查或恢复历史配置。
- [反馈编号：无] 小程序配置中心优化树节点命名：将首页 `tools` 模块展示为“首页快捷入口”，将独立工具页展示为“底部 Tab：工具页”，工具页下分组显示为“工具页分组”，降低“首页工具类”和“工具页”概念混淆。
- [反馈编号：无] 将后台菜单入口和页面标题从“小程序配置中心”调整为“页面菜单管理”，页面主结构改为业务人员可理解的 `Tab / 模块 / 功能` 三层菜单树，不再用“首页快捷入口 / 底部 Tab 工具页”等临时文案掩盖结构问题。
- [反馈编号：无] 页面菜单管理继续保留“显示停用项”和功能编辑能力，功能节点支持编辑标题、说明、页面路径、动作、业务状态、状态文案、图标、可见性、发布阶段、排序和启用状态；技术来源仅在详情元信息中体现，后续可在功能下扩展按钮权限控制。
- [反馈编号：无] 页面菜单管理适配后端压平后的工具类菜单：不再把 `诉讼计算`、`规则与指引` 等法律工具分组当作菜单模块展示，法律工具功能直接显示在“常用功能 -> 工具类”下；树节点新增展开/收缩控制，便于查看层级叶子数据。
- [反馈编号：无] 优化页面菜单管理视觉层级：左侧树增加层级色条、节点卡片 hover/active 态、子项数量徽标和更清晰的折叠控件；右侧详情区增加节点概览条和菜单层级/展示状态/目标页面摘要，降低业务人员查看菜单结构的理解成本。

## 2026-05-31

- [反馈编号：无] 新增“小程序配置中心”菜单与路由 `/miniapp-orchestration`，查看权限为 `admin:miniapp-home-config:view`，用于按“小程序 -> 页面 -> 模块 -> 功能入口”查看对客展示编排。
- [反馈编号：无] 新增 `src/api/miniappOrchestration.ts`，封装 `POST /api/admin/miniapp-orchestration/tree` 与 `POST /api/admin/miniapp-orchestration/entries/save`，管理端继续只通过 `miniapp-backend` 受控 API 管理配置。
- [反馈编号：无] 新增小程序配置中心页面，首片聚合首页配置入口与法律工具曝光入口；支持选择入口后编辑标题、说明、页面路径、动作、业务状态、状态文案、图标、可见性、发布阶段、排序和启用状态，并复用共享开源图标选择器。
- [反馈编号：无] 共享 `MiniappIconPicker` 支持只读禁用态，小程序配置中心在账号缺少对应管理权限时禁止本地图标选择变更，避免只读页面出现可编辑错觉。
- [反馈编号：无] TDD 验证：RED 阶段 API 测试失败于 `miniappOrchestration` 模块缺失，路由测试失败于菜单/路由缺失，页面测试失败于组件缺失；GREEN 阶段 `npm.cmd run test -- --run src/api/miniappOrchestration.test.ts src/router/router.test.ts src/pages/miniapp-orchestration/MiniappOrchestrationPage.test.ts` 通过，3 个测试文件、20 项 Vitest。
- [反馈编号：无] 收口验证：`npm.cmd run quality` 通过，32 个测试文件、135 项 Vitest，`vue-tsc --noEmit` 和 `vite build` 通过；构建仍保留既有 Rollup PURE 注释 warning 和 chunk size warning。

- [反馈编号：LMA-FB-029] 法律工具中心测试样本新增 `wage_conversion` 能力、曝光入口和官方来源记录，覆盖“工资折算”标题、`badge-dollar-sign` 图标、`/pages/wage-conversion/wage-conversion` 跳转、`miniapp_local_calculation` 执行方式、`low` 风险等级和 `mohrss-2025-2-wage-conversion` 来源版本；本阶段不新增独立规则维护页。
- [反馈编号：LMA-FB-029] 定向验证已通过：`npm.cmd test -- LegalToolCenterPage.test.ts`，1 个测试文件、10 项 Vitest。
- [反馈编号：LMA-FB-029] 收口验证：`npm.cmd run quality` 通过，30 个测试文件、130 项 Vitest，`vue-tsc --noEmit` 和 `vite build` 通过；构建仍保留既有 Rollup PURE 注释和 chunk size warning。

- [反馈编号：LMA-FB-028] 法律工具中心测试样本新增 `level_jurisdiction` 能力和曝光入口，覆盖“级别管辖核对”标题、`landmark` 图标、`/pages/level-jurisdiction/level-jurisdiction` 跳转和 `miniapp_local_calculation` 执行方式；本阶段不新增独立规则维护页。

- [反馈编号：LMA-FB-026] 法律工具中心测试样本新增 `economic_compensation` 能力和曝光入口，覆盖“经济补偿金”标题、`badge-dollar-sign` 图标、`/pages/economic-compensation/economic-compensation` 跳转和 `miniapp_local_calculation` 执行方式；本阶段不新增独立规则维护页。

- [反馈编号：LMA-FB-025] 法律工具中心测试样本新增 `delayed_performance_interest` 能力和曝光入口，覆盖“延迟履行利息”标题、`timer-reset` 图标和 `/pages/delayed-performance-interest/delayed-performance-interest` 跳转；本阶段不新增独立规则维护页。

- [反馈编号：LMA-FB-024] 法律工具中心页面测试补充 `civil_cause_of_action` 能力和曝光入口样例，覆盖“民事案由”在能力库与曝光入口中展示，目标路径为 `/pages/civil-cause/civil-cause`，图标为 `search-check`，执行方式为 `backend_data`。
- [反馈编号：LMA-FB-024] 本轮不新增独立 admin 页面；复用既有法律工具中心管理能力、来源和曝光入口配置能力。
- [反馈编号：LMA-FB-024] 定向验证已通过：`npm.cmd test -- LegalToolCenterPage.test.ts`，1 个测试文件、10 项 Vitest。
- [反馈编号：LMA-FB-024] 收口验证：`npm.cmd run quality` 通过，30 个测试文件、130 项 Vitest，`vue-tsc --noEmit` 和 `vite build` 通过；`git diff --check` 无空白错误，仅提示 Windows 换行转换。构建仍保留既有 Rollup PURE 注释和 chunk size warning。

- [反馈编号：LMA-FB-023] 法律工具中心页面测试补充 `paid_annual_leave` 能力和曝光入口样例，覆盖“带薪年休假”在能力库与曝光入口中展示，目标路径为 `/pages/paid-annual-leave/paid-annual-leave`，图标为 `calendar-check`。
- [反馈编号：LMA-FB-023] 本轮不新增独立 admin 页面；复用既有法律工具中心管理能力配置能力、来源和曝光入口。定向验证已通过：`npm.cmd test -- LegalToolCenterPage.test.ts`，1 个测试文件、10 项 Vitest。
- [反馈编号：LMA-FB-023] 收口验证：`npm.cmd run quality` 通过，30 个测试文件、130 项 Vitest，`vue-tsc --noEmit` 和 `vite build` 通过；`git diff --check` 无空白错误，仅提示 Windows 换行转换。构建仍保留既有 Rollup PURE 注释和 chunk size warning。

- [反馈编号：LMA-FB-022] 法律工具中心页面测试补充 `date_calculation` 能力和曝光入口样例，覆盖“日期推算”在能力库与曝光入口中展示，目标路径为 `/pages/date-calculation/date-calculation`，图标为 `calendar-days`。
- [反馈编号：LMA-FB-022] 本轮不新增独立 admin 页面；复用既有法律工具中心管理能力配置能力、来源和曝光入口。定向验证已通过：`npm.cmd test -- LegalToolCenterPage.test.ts`，1 个测试文件、10 项 Vitest。
- [反馈编号：LMA-FB-022] 收口验证：`npm.cmd run quality` 通过，30 个测试文件、130 项 Vitest，`vue-tsc --noEmit` 和 `vite build` 通过；`git diff --check` 无空白错误，仅提示 Windows 换行转换。

- [反馈编号：LMA-FB-021] 法律工具中心页面测试补充 `private_lending_interest` 能力和曝光入口样例，覆盖“新民间借贷利息”在能力库与曝光入口中展示，目标路径为 `/pages/private-lending-interest/private-lending-interest`。
- [反馈编号：LMA-FB-021] 本轮不新增独立 admin 页面；复用既有法律工具中心管理能力配置能力、来源和曝光入口。定向验证已通过：`npm.cmd test -- LegalToolCenterPage.test.ts`，1 个测试文件、10 项 Vitest。
- [反馈编号：LMA-FB-021] 管理端收口验证：`npm.cmd run quality` 通过，30 个测试文件、130 项 Vitest，`vue-tsc --noEmit` 和 `vite build` 通过；`git diff --check` 无空白错误，仅提示 Windows 换行转换。

- [反馈编号：LMA-FB-020] admin 侧诉讼费用规则适配后端多规则契约：`LitigationFeeBand` 兼容 `feeMin/feeMax/excessBase/excessRate`，`LitigationFeePreviewResult` 兼容区间费用 `feeMin/feeMax`，保留旧 `fixedFee/rate/quickAdjustment/fee` 单值规则展示。
- [反馈编号：LMA-FB-020] 法律工具中心诉讼费用规则列表覆盖 V23 至少 6 条规则展示，弹窗分段编辑新增费用下限、费用上限、超额基数和超额费率字段；保存 payload 显式带回新增字段且继续不回传审计字段。
- [反馈编号：LMA-FB-020] 诉讼费用预览结果支持离婚案件等区间规则：响应存在 `feeMin/feeMax` 时展示费用区间，否则继续展示单值 `fee`；定向 TDD 验证覆盖 2 个测试文件、16 项 Vitest，RED 阶段失败于页面缺少新区间字段和区间预览展示，GREEN 阶段通过。

- [反馈编号：LMA-FB-020] 法律工具中心新增“诉讼费用规则”管理标签页，接入 `POST /api/admin/legal-tool-center/litigation-fee-rules/page|save|preview` 与 `POST /api/admin/legal-tool-center/litigation-fee-rules/{id}/publish`，用于维护财产案件受理费规则、分段、来源、核验日期、预览金额和发布动作。
- [反馈编号：LMA-FB-020] `src/api/legalToolCenter.ts` 新增诉讼费用规则 API client、分段类型、规则 payload 和预览结果类型；页面沿用法律工具中心后台表格和弹窗风格，不提供后端接口实现，不直连数据库。
- [反馈编号：LMA-FB-020] TDD 验证覆盖 API 封装、页面规则列表加载、预览 100000 元返回 2300、保存 payload 不回传审计字段、发布前确认；RED 阶段分别失败于缺失 `pageLitigationFeeRules` 和缺失页面 loader/method。

- [反馈编号：LMA-FB-019] 新增“数据同步/发布”菜单与路由 `/data-governance`，查看权限为 `admin:data-governance:view`；管理端继续只通过 `miniapp-backend` 受控 API 管理业务数据，不直连数据库，不控制 crawler 进程。
- [反馈编号：LMA-FB-019] 新增 `src/api/dataGovernance.ts`，封装 `POST /api/admin/data-governance/lpr-sync-batches/page`、`/lpr-rate-revisions/page` 和 `/lpr-rates/sync`，用于查询 LPR 同步批次、修订记录和发布受控 LPR JSON。
- [反馈编号：LMA-FB-019] 新增数据治理页面，默认 `appCode` 为 `lawsuit-material-assistant`，包含“同步批次 / 修订记录 / LPR JSON 发布”三个标签页；JSON 发布 textarea 标记 `data-test="lpr-sync-json"`，发布按钮标记 `data-test="publish-lpr-sync"`，且在前端校验缺少 `items` 数组时直接报错。
- [反馈编号：LMA-FB-019] TDD 验证覆盖数据治理 API 封装、菜单/路由权限、页面加载批次/修订和发布 JSON 后刷新批次；定向命令通过 3 个测试文件、21 项 Vitest，全量 `npm.cmd run quality` 通过 30 个测试文件、127 项 Vitest 与 `vue-tsc --noEmit && vite build`，`git diff --check` 退出 0（仅 Windows 换行 warning）。

- [反馈编号：LMA-FB-018] 法律工具中心新增“LPR利率”维护标签页，接入 `miniapp-backend` 的 `POST /api/admin/legal-tool-center/lpr-rates/page|save`，用于维护报价日期、一年期 LPR、五年期以上 LPR、来源标识、来源链接、来源版本、核验日期、状态、排序和启用状态。
- [反馈编号：LMA-FB-018] `src/api/legalToolCenter.ts` 新增 `pageLegalLprRates`、`saveLegalLprRate` 和对应类型；页面保存时继续显式构造 payload，不回传 `createdAt/updatedAt` 等审计字段，不提供任意 WXML/HTML/CSS/JS 编辑能力。
- [反馈编号：LMA-FB-018] TDD 验证覆盖 LPR API 封装和页面加载、编辑、保存流程；定向命令 `npm.cmd run test -- --run src/api/legalToolCenter.test.ts src/pages/legal-tool-center/LegalToolCenterPage.test.ts` 通过，2 个测试文件、13 项 Vitest 测试通过。
- [反馈编号：LMA-FB-018] 收口全量质量验证：`npm.cmd run quality` 通过，28 个测试文件、120 项 Vitest 测试通过，`vue-tsc --noEmit && vite build` 通过；构建保留既有 Rollup PURE 注释 warning 和 chunk size warning。
- [反馈编号：LMA-FB-018] LPR 管理端提交 `f88d0de feat: add legal lpr rate admin` 已推送，企业微信智能表格 `HaYpcv` 已回写为“已验证（LPR 查询小闭环通过，已推送）”；下一轮进入数据驱动治理设计，评估同步批次、人工复核、冲突处理和公网发布数据管理入口。

## 2026-05-30

- [反馈编号：LMA-FB-018] 新增“法律工具中心”菜单与路由 `/legal-tool-center`，查看权限为 `admin:legal-tool-center:view`，保存和禁用入口按 `admin:legal-tool-center:manage` 控制。
- [反馈编号：LMA-FB-018] 新增 `src/api/legalToolCenter.ts`，封装 `miniapp-backend` 的 `POST /api/admin/legal-tool-center/capabilities/page|save`、`groups/page|save|{groupId}/disable`、`exposure-items/page|save|{itemId}/disable`；管理端不直连数据库。
- [反馈编号：LMA-FB-018] 新增法律工具中心管理页，按“能力库 / 展示分组 / 曝光入口”维护竞品吸收工具、来源等级、风险等级、执行方式、可见性、发布阶段和小程序页面指向；曝光入口和能力默认图标复用共享开源图标库 `MiniappIconPicker`。
- [反馈编号：LMA-FB-018] 页面保存逻辑显式构造结构化 payload，避免将后端 `createdAt/updatedAt` 等审计字段回传；页面不提供任意 WXML/HTML/CSS/JS 编辑能力。
- [反馈编号：LMA-FB-018] TDD 验证：`npm.cmd run test -- --run src/api/legalToolCenter.test.ts` 红灯失败于 API 模块缺失，绿灯 1 个测试文件、3 个 Vitest 测试通过；`npm.cmd run test -- --run src/pages/legal-tool-center/LegalToolCenterPage.test.ts` 红灯失败于页面组件缺失，绿灯 1 个测试文件、6 个 Vitest 测试通过；`npm.cmd run test -- --run src/router/router.test.ts` 红灯失败于菜单/路由缺失，绿灯 1 个测试文件、14 个 Vitest 测试通过。
- [反馈编号：LMA-FB-018] 定向汇总验证：`npm.cmd run test -- --run src/api/legalToolCenter.test.ts src/pages/legal-tool-center/LegalToolCenterPage.test.ts src/router/router.test.ts` 通过，3 个测试文件、23 个 Vitest 测试通过。
- [反馈编号：LMA-FB-018] 全量质量验证：`npm.cmd run quality` 通过，28 个测试文件、116 个 Vitest 测试通过，`vue-tsc --noEmit && vite build` 通过；构建保留既有 Rollup PURE 注释 warning 和 chunk size warning。`git diff --check` 通过，仅提示 Windows 换行转换 warning。
- [反馈编号：LMA-FB-018] 法律工具中心管理页新增“数据来源”和“交互蓝图”两个维护标签页；`src/api/legalToolCenter.ts` 新增 `data-sources/page|save`、`interaction-blueprints/page|save` 四个受控后台 API 封装。
- [反馈编号：LMA-FB-018] 数据来源页支持维护来源标识、名称、类型、发布机构、HTTPS 来源链接、引用文号、生效日期、版本、核验日期、状态、风险等级和关联工具；交互蓝图页支持维护蓝图标识、工具标识、参考类型、表单分组 JSON、结果区块 JSON、CTA 规则 JSON、校验说明、审核信息和启用状态。
- [反馈编号：LMA-FB-018] 页面继续显式构造保存 payload，不回传 `createdAt/updatedAt`，不提供任意 WXML/HTML/CSS/JS 编辑能力。RED 阶段 API 测试失败于 `pageLegalToolDataSources is not a function`，页面测试失败于新 loader/dialog 缺失；GREEN 后 `npm.cmd run test -- --run src/pages/legal-tool-center/LegalToolCenterPage.test.ts src/api/legalToolCenter.test.ts` 通过，2 个测试文件、11 个 Vitest 测试通过。
- [反馈编号：LMA-FB-018] 收口全量质量验证：`npm.cmd run quality` 通过，28 个测试文件、118 个 Vitest 测试通过，`vue-tsc --noEmit && vite build` 通过；构建保留既有 Rollup PURE 注释 warning 和 chunk size warning。`git diff --check` 无空白错误，仅提示 Windows 换行转换。

- [反馈编号：LMA-FB-016] 新增“结果模板配置”菜单与路由 `/private-lending-result-template`，查看权限为 `admin:private-lending-result-template:view`，保存按钮按 `admin:private-lending-result-template:manage` 控制。
- [反馈编号：LMA-FB-016] 新增 `src/api/privateLendingResultTemplate.ts`，封装 `GET /api/admin/private-lending-result-template`、`POST /save` 和 `POST /preview`；管理端只通过 `miniapp-backend` 受控 API 读写模板，不直连数据库。
- [反馈编号：LMA-FB-016] 新增民间借贷结果模板配置页，支持维护草稿标题、风险提示、服务 CTA、证据清单、立案提示和草稿正文行模板；预览面板展示后端返回的结构化 `docPackage`，不渲染任意 HTML/WXML/CSS/JS。
- [反馈编号：LMA-FB-016] TDD 验证：RED 阶段 `npm.cmd run test -- --run src/api/privateLendingResultTemplate.test.ts src/pages/private-lending-result-template/PrivateLendingResultTemplatePage.test.ts src/router/router.test.ts` 失败于 API/页面/路由缺失；GREEN 阶段同命令通过，3 个测试文件、18 个 Vitest 测试通过。
- [反馈编号：LMA-FB-016] 全量质量验证：`npm.cmd run quality` 通过，26 个测试文件、106 个 Vitest 测试通过，`vue-tsc --noEmit && vite build` 通过；构建保留既有 Rollup PURE 注释 warning 和 chunk size warning。
- [反馈编号：LMA-FB-016] 真实浏览器闭环：启动当前后端和 `npm.cmd run dev -- --port 5173` 后，使用 `admin/123456` 登录后台，侧边栏可见“结果模板配置”；进入 `/private-lending-result-template` 后成功加载后端模板，点击“预览结果”展示后端返回的 `docPackage`，包含“李四向张三出借50000元”的样例正文、证据清单、立案提示和风险提示。

- [反馈编号：LMA-FB-013] 将首页配置页内的局部图标候选抽象为后台共享 `miniappIconCatalog` 和 `MiniappIconPicker` 组件，统一管理小程序配置页使用的开源图标候选。
- [反馈编号：LMA-FB-013] 首页配置页和文书目录配置页均接入共享图标选择器，配置人员不再需要在文书目录页手写 `iconKey`；后台保存字段仍为既有 `iconKey` 字符串，不改变后端接口结构。
- [反馈编号：LMA-FB-013] 共享图标目录继续使用 `@element-plus/icons-vue` 开源图标组件作为后台预览，不新增依赖；后续其他小程序配置页可直接复用同一选择器。
- [反馈编号：LMA-FB-013] 定向验证：`npm.cmd run test -- --run src/miniapp-icons/miniappIconCatalog.test.ts src/pages/miniapp-home-config/MiniappHomeConfigPage.test.ts src/pages/miniapp-document-catalog/MiniappDocumentCatalogPage.test.ts` 通过，3 个测试文件、10 个 Vitest 测试通过。
- [反馈编号：LMA-FB-013] 全量质量验证：`npm.cmd run quality` 通过，24 个测试文件、100 个 Vitest 测试通过，`vue-tsc --noEmit && vite build` 通过；构建保留既有 Rollup PURE 注释 warning 和 chunk size warning。`git diff --check` 仅提示 Windows 换行转换。

## 2026-05-29

- [反馈编号：LMA-FB-014] 收口 checkpoint 状态：法律助手同局域网实体手机已确认起诉文书生成页读取后台配置结果，展示 1 个开放入口“民间借贷纠纷”和 2 个“暂不可生成”入口；企业微信智能表格 record_id=`OfxNNC` 已回写为“已验证（同局域网真机抽检通过）”，本仓下一步转为提交前验证和提交整理。
- [反馈编号：LMA-FB-014] 配合后端多小程序作用域收紧：文书目录禁用请求现在携带当前 `appCode`，页面测试覆盖调用 `disableMiniappDocumentCatalogItem(1, 'lawsuit-material-assistant')` 后刷新列表；定向 `npm.cmd run test -- --run src/api/miniappDocumentCatalog.test.ts src/pages/miniapp-document-catalog/MiniappDocumentCatalogPage.test.ts` 通过，2 个测试文件、5 个 Vitest 测试通过。
- [反馈编号：LMA-FB-014] 收口全量质量验证：`npm.cmd run quality` 通过，23 个测试文件、97 个 Vitest 测试通过，`vue-tsc --noEmit && vite build` 通过；构建保留既有 Rollup PURE 注释 warning 和 chunk size warning。

## 2026-05-28

- [反馈编号：LMA-FB-014] 新增文书目录配置管理页 `/miniapp-document-catalog`，通过 `miniapp-backend` 后台 CRUD API 管理起诉文书生成目录项，不直连数据库、不绕过后端写业务数据。
- [反馈编号：LMA-FB-014] 新增 `src/api/miniappDocumentCatalog.ts`，封装 `POST /api/admin/miniapp-document-catalog/items/page|save` 和 `POST /api/admin/miniapp-document-catalog/items/{itemId}/disable?appCode=...`；页面支持维护 `appCode/caseType/title/description/targetPath/action/status/statusText/iconKey/sortOrder/enabled`。
- [反馈编号：LMA-FB-014] 菜单和路由新增“文书目录配置”，查看权限为 `admin:miniapp-document-catalog:view`，保存和禁用按钮按 `admin:miniapp-document-catalog:manage` 控制；`npm.cmd run admin:check` 同步检查新路由、API 模块和页面模块。
- [反馈编号：LMA-FB-014] 新增 `miniappDocumentCatalog` API 测试、`MiniappDocumentCatalogPage` 页面测试，并扩展路由和联调准备诊断测试；RED 验证失败于 API/页面/路由缺失，GREEN 验证 `npm.cmd run test -- --run src/api/miniappDocumentCatalog.test.ts src/pages/miniapp-document-catalog/MiniappDocumentCatalogPage.test.ts src/router/router.test.ts` 通过，3 个测试文件、16 个 Vitest 测试通过。
- [反馈编号：LMA-FB-014] 全量质量验证：`npm.cmd run quality` 通过，23 个测试文件、96 个 Vitest 测试通过，`vue-tsc --noEmit && vite build` 通过；构建保留既有 Rollup PURE 注释 warning 和 chunk size warning。
- [反馈编号：LMA-FB-014] 完成真实浏览器联调：重新启动当前代码后端后，`admin/123456` 登录可见“文书目录配置”，页面加载 `private_lending/divorce/labor` 三条正式目录；通过页面新增 `codex_check_20260528` 测试目录并立即软禁用，表格显示该记录为“禁用”，公开小程序目录接口确认不返回该禁用测试记录。
- [反馈编号：LMA-FB-013] 同步 checkpoint 状态：管理端真实后端账号联调、小程序真实接口自动化复验和同局域网实体手机首页配置抽检均已通过，首页 Banner 与 Lucide 风格图标显示正常。

## 2026-05-27

- [反馈编号：LMA-FB-013] 新增法律助手首页配置管理页 `/miniapp-home-config`，通过 `miniapp-backend` 后台 CRUD API 管理首页模块、功能入口和 Banner 公告，不直连数据库、不绕过后端写业务数据。
- [反馈编号：LMA-FB-013] 新增 `src/api/miniappHomeConfig.ts`，封装 `POST /api/admin/miniapp-home-config/modules/page|save`、`/modules/{moduleId}/disable`、`/menu-items/page|save`、`/menu-items/{itemId}/disable`、`/banners/page|save`、`/banners/{bannerId}/disable`。
- [反馈编号：LMA-FB-013] 管理页按“模块配置 / 功能入口 / Banner 公告”分区展示和维护；菜单与路由新增 `/miniapp-home-config`，查看权限为 `admin:miniapp-home-config:view`，新增、编辑、禁用按钮按 `admin:miniapp-home-config:manage` 控制。
- [反馈编号：LMA-FB-013] 新增 `miniappHomeConfig` API 测试、`MiniappHomeConfigPage` 页面测试，并扩展路由测试；RED 验证失败于 API/页面/路由缺失，GREEN 验证 `npm.cmd run test -- --run src/api/miniappHomeConfig.test.ts src/pages/miniapp-home-config/MiniappHomeConfigPage.test.ts src/router/router.test.ts` 通过，3 个测试文件、17 个 Vitest 测试通过。
- [反馈编号：LMA-FB-013] 全量质量验证：`npm.cmd run quality` 通过，21 个测试文件、91 个 Vitest 测试通过，`vue-tsc --noEmit && vite build` 通过；构建保留既有 Rollup PURE 注释 warning 和 chunk size warning。
- [反馈编号：LMA-FB-013] 管理端首页配置页已提交并推送：`fcd220c feat: add miniapp home config admin page`；企业微信智能表格记录 `XRhKT7` 已回写为“已验证（后台配置页已完成，待真实联调）”，下一步用真实后端账号联调新增、编辑和禁用。
- [反馈编号：LMA-FB-013] 完成真实后端账号浏览器联调：重启旧 8080 后端进程后，`admin/123456` 登录返回 `admin:miniapp-home-config:view/manage`，管理端展示“首页配置”；`/miniapp-home-config` 页面完成模块、功能入口、Banner 的新增、编辑和禁用，测试记录均已软禁用；公开首页配置接口确认不返回已禁用的 `codex_test_*` 记录。
- [反馈编号：LMA-FB-009] 新增后台真实浏览器联调前置诊断命令 `npm.cmd run admin:check`，检查 `miniapp-backend` 健康接口、Vite `/api` 代理、服务请求/操作审计/生成记录/用户等关键路由，以及服务请求和操作审计关键 API/页面模块。
- [反馈编号：LMA-FB-009] 新增 `scripts/check-admin-integration-ready.test.mjs`，覆盖诊断汇总、后端不可达 WARN 不阻断、命令行输出格式，便于远程状态下先完成联调准备检查。
- [反馈编号：LMA-FB-009] 本轮验证：红灯 `npm.cmd run test -- --run scripts/check-admin-integration-ready.test.mjs` 失败于诊断脚本缺失；绿灯同命令 3 个 Vitest 测试通过；`npm.cmd run admin:check` 通过，当前报告 PASS 11 / WARN 0 / FAIL 0；`npm.cmd run quality` 通过，19 个测试文件、84 个 Vitest 测试通过，`vue-tsc --noEmit && vite build` 通过，构建保留既有 Rollup PURE 注释 warning 和 chunk size warning。

## 2026-05-26

- [反馈编号：LMA-FB-009] 同步本地后端 API 联调结果：`miniapp-backend` 当前仓库 `p15` profile 已在 `localhost:8080` 跑通 `contract_template` 服务请求创建、重复提交 409、后台分页、详情脱敏、`contact-view`、状态处理和 `legal_service_request_contact_view` 审计日志；管理后台页面仍待真实浏览器联调和生产级权限账号复核。
- [反馈编号：LMA-FB-009] 复跑后台质量检查：`npm.cmd run quality` 通过，18 个测试文件、81 个 Vitest 测试通过，`vue-tsc --noEmit && vite build` 通过；构建保留既有 Rollup PURE 注释 warning 和 chunk size warning。
- [反馈编号：LMA-FB-009] 后台服务请求页同步法律助手服务请求契约，服务类型筛选新增“合同模板”选项并下发 `serviceType: "contract_template"`，未引入订单、支付、收款等无关表达。
- [反馈编号：LMA-FB-009] 修正 checkpoint 文档中“已完成未提交”的陈旧状态，更新为后台 MVP+ 已提交已推送、等待真实后端联调。

## 2026-05-24

- [反馈编号：无] 新增后台“操作审计”页面 `/user-operation-logs`，作为服务请求联系方式审计后的查询入口；权限码 `admin:user-operation-log:view`，支持按用户 ID 和操作类型查询用户操作日志。
- [反馈编号：无] 操作审计页复用 `POST /api/admin/user-operation-logs/page`，操作类型筛选包含全部、用户状态变更、服务请求状态变更和服务请求联系方式查看 `legal_service_request_contact_view`；用户 ID 仅正整数且不超过 JS 安全整数范围时下发。
- [反馈编号：无] 操作审计页补齐防御性脱敏：`legal_service_request_contact_view` 的 Before/After 值即使收到历史或异常完整手机号，也在前端二次脱敏后展示；查询请求显式携带 `orderBy=createdAt`，保持与后台分页契约一致。
- [反馈编号：无] 扩展路由/菜单测试并新增 `src/pages/user-operation-logs/UserOperationLogsPage.test.ts`，覆盖初始加载、筛选查询、非法用户 ID 边界和表格字段展示。
- [反馈编号：无] 验证通过：红灯 `npm.cmd run test -- --run src/pages/user-operation-logs/UserOperationLogsPage.test.ts src/router/router.test.ts` 失败于页面组件不存在、菜单和路由未声明 `/user-operation-logs`；绿灯同命令通过，2 个测试文件、18 个 Vitest 测试通过；最终 `npm.cmd run quality` 通过，18 个测试文件、80 个 Vitest 测试通过，`vue-tsc --noEmit && vite build` 通过，保留既有 Rollup PURE 注释 warning 和 chunk size warning。
- [反馈编号：无] 后台服务请求详情联系方式查看改为显式动作：普通详情和状态更新响应默认展示 `contactPhoneMasked`，点击“查看完整手机号”后调用 `POST /api/admin/legal/service-requests/{requestId}/contact-view` 获取完整 `contactPhone`，适配后端审计接口；查看失败在详情联系人区域提示错误。
- [反馈编号：无] 同步管理后台项目状态：`AGENTS.md`、`tasks/current-task.md` 和 `codex-handoff.md` 从占位/已完成未提交修正为后台 MVP+ 已提交已推送，等待真实后端联调和下一阶段业务页。
- [反馈编号：无] 用户管理页空筛选归一化：空 `keywords`、`status`、`appCode` 下发 `undefined`，继续保留合法 `userId` 精确筛选和 JS 安全整数边界；`UsersPage.test.ts` 覆盖空筛选不下发和合法 userId 仍下发。
- [反馈编号：无] 补齐管理后台反馈编号追溯约束：更新 `AGENTS.md`，要求来自企业微信文档或产品反馈台账的改动在任务文档、变更日志、验证记录、提交信息和阶段回写中保留同一反馈编号；无产品反馈编号的工程维护写 `Refs: none`。
- 新增服务请求管理页 `/legal-service-requests`，通过 `miniapp-backend` 的 `POST /api/admin/legal/service-requests/page` 查询服务请求，不直连数据库、不控制 crawler。
- 新增 `src/api/legalServiceRequests.ts`，封装服务请求分页、详情和状态更新接口；详情接口读取完整手机号，状态更新调用 `POST /api/admin/legal/service-requests/{requestId}/status`。
- 主菜单和路由新增“服务请求”，权限码 `admin:legal-service-request:view`；管理操作入口按 `admin:legal-service-request:manage` 控制。
- 服务请求页支持关键词、用户 ID、手机号、服务类型、状态和小程序筛选；空筛选统一传 `undefined`，用户 ID 仅正整数且 `Number.isSafeInteger` 时下发。
- 服务请求页展示请求 ID、用户 ID、小程序、服务类型、联系人、脱敏手机号、状态、处理人、创建/更新时间和详情操作；详情抽屉展示完整手机号、用户备注、内部备注、来源记录，并支持跳转 `/users?userId=...` 和 `/generation-records?userId=...`。
- 新增 `src/api/legalServiceRequests.test.ts`、`src/pages/legal-service-requests/LegalServiceRequestsPage.test.ts`，扩展 `src/router/router.test.ts`，按 TDD 覆盖 API 契约、菜单/路由权限、筛选归一化、非法用户 ID、查看用户跳转和管理权限状态更新。
- 验证通过：红灯 `npm.cmd run test -- --run src/api/legalServiceRequests.test.ts src/pages/legal-service-requests/LegalServiceRequestsPage.test.ts src/router/router.test.ts` 失败于缺少 API/页面模块和路由菜单声明；绿灯同命令通过，3 个测试文件、22 个 Vitest 测试通过；最终 `npm.cmd run quality` 通过，17 个测试文件、66 个 Vitest 测试通过，`vue-tsc --noEmit && vite build` 通过，保留既有 Rollup PURE 注释 warning 和 chunk size warning。
- 用户管理页将 `/users?userId=...` 从“关键词排查”升级为后端 `userId` 精确筛选：合法正整数且不超过 JS 安全整数范围的 query 会填入独立用户 ID 筛选框，请求 `POST /api/admin/users/page` 时下发 `userId`，并保持 `keywords` 为空。
- `AdminUserPageQuery` 增加 `userId?: number`，`src/api/adminUsers.test.ts` 覆盖分页请求体携带精确用户 ID。
- `UsersPage` 新增“用户ID”筛选项，保留关键词、状态、小程序筛选；非法、空白、小数、负数、超出 JS 安全整数范围和空数组 `userId` query 不下发 `userId`，避免污染查询条件或静默丢精度。
- 更新 `UsersPage.test.ts`，覆盖合法 query、数组 query、非法 query、关键词不污染和页面提示文案。
- 验证通过：`npm.cmd run test -- --run src/pages/users/UsersPage.test.ts src/api/adminUsers.test.ts`，2 个测试文件、13 个测试通过；相关定向 4 个测试文件、23 个测试通过；`npm.cmd run quality`，15 个测试文件、52 个测试通过，`vue-tsc --noEmit && vite build` 通过，保留既有 Rollup PURE 注释 warning 和 chunk size warning。

## 2026-05-23

- 生成记录页和法律表单事件页在用户 ID 列新增“查看用户”动作，跳转 `/users?userId=...` 作为跨页排查入口；不改生成记录、法律表单事件或用户分页后端接口契约。
- 用户管理页读取 `/users?userId=...` 查询参数，仅在合法正整数时填入现有 `keywords` 条件查询用户列表，并展示“关键词排查”提示，避免假造后端 `userId` 精确筛选能力。
- 扩展 `src/pages/generation-records/GenerationRecordsPage.test.ts`、`src/pages/legal-form-events/LegalFormEventsPage.test.ts`，新增 `src/pages/users/UsersPage.test.ts`，按 TDD 覆盖来源页跳转、用户页 query 初始化排查入口，以及非法、空白、小数、负数和数组 `userId` query 边界。
- 追加定向验证：`npm.cmd run test -- --run src/pages/users/UsersPage.test.ts src/pages/generation-records/GenerationRecordsPage.test.ts src/pages/legal-form-events/LegalFormEventsPage.test.ts` 通过，3 个测试文件、18 个 Vitest 测试通过。
- 验证通过：红灯 `npm.cmd run test -- --run src/pages/generation-records/GenerationRecordsPage.test.ts src/pages/legal-form-events/LegalFormEventsPage.test.ts src/pages/users/UsersPage.test.ts` 失败于缺少“查看用户”跳转和 query 关键词初始化；绿灯同命令通过，3 个测试文件、11 个 Vitest 测试通过；相关定向 `npm.cmd run test -- --run src/pages/generation-records/GenerationRecordsPage.test.ts src/pages/legal-form-events/LegalFormEventsPage.test.ts src/pages/users/UsersPage.test.ts src/router/router.test.ts src/api/adminUsers.test.ts` 通过，5 个测试文件、23 个 Vitest 测试通过；最终 `npm.cmd run quality` 通过，15 个测试文件、51 个 Vitest 测试通过，TypeScript 与 Vite 构建通过；构建保留既有 Rollup 注释 warning 和 chunk size warning。
- 后台路由守卫补齐刷新恢复：本地存在 `cekaitech-admin-token` 但 Pinia `operator` 缺失时，先调用已有 `getCurrentOperator` / `/api/admin/auth/current-operator` 恢复 operator 与 permissions，再执行权限判断；恢复失败时清理 token 并回到登录页。
- 法律表单事件页补充组件测试 `src/pages/legal-form-events/LegalFormEventsPage.test.ts`，覆盖初始加载空筛选归一化、关键词/用户 ID/状态/事件类型/小程序筛选和非法用户 ID。
- 法律表单事件页查询参数对齐生成记录页：空关键词、小程序、表单类型、质量状态和事件类型统一传 `undefined`，用户 ID 仅正整数下发；新增用户 ID 和事件类型筛选控件，不改后端接口路径。
- 验证通过：`npm.cmd run test -- --run src/stores/auth.test.ts src/api/adminAuth.test.ts src/api/http.test.ts src/api/legalFormEvents.test.ts src/pages/legal-form-events/LegalFormEventsPage.test.ts src/router/router.test.ts` 通过，6 个测试文件、18 个 Vitest 测试通过；`npm.cmd run quality` 通过，14 个测试文件、41 个 Vitest 测试通过，TypeScript 与 Vite 构建通过；构建保留既有 Rollup 注释 warning 和 chunk size warning。
- 生成记录页补充组件交互测试 `src/pages/generation-records/GenerationRecordsPage.test.ts`，mock 分页 API 覆盖初始加载、列表字段渲染、筛选查询、重置和非法用户 ID 场景。
- 生成记录页查询参数归一化：空关键词、小程序、状态和记录类型统一传 `undefined`，用户 ID 仅正整数传给后端，避免空字符串和 `NaN` 影响联调。
- 生成记录类型筛选对齐法律助手小程序实际 `caseType`：补齐 `divorce`、`labor`、`contract`、`tort` 展示与筛选，避免通用模板云端记录在后台只能显示原始枚举。
- 验证通过：`npm.cmd run test -- --run src/pages/generation-records/GenerationRecordsPage.test.ts` 通过，1 个测试文件、5 个 Vitest 测试通过；`npm.cmd run test -- --run src/pages/generation-records/generationRecordOptions.test.ts` 通过，3 个测试通过；`npm.cmd run quality` 通过，13 个测试文件、36 个 Vitest 测试通过，TypeScript 与 Vite 构建通过；构建保留既有 Rollup 注释 warning 和 chunk size warning。
- 新增生成记录后台页面 `/generation-records`，通过 `miniapp-backend` 的 `POST /api/admin/generation-records/page` 查询法律助手小程序云端生成记录，不直连数据库、不接 crawler。
- 新增 `src/api/generationRecords.ts`，封装生成记录分页接口；请求字段使用 `pageNo`、`pageSize`、`appCode`、`userId`、`status`、`recordType`、`keywords`、`orderBy`、`order`，返回沿用 `dataList`、`totalCount`。
- 新增 `src/api/generationRecords.test.ts`，按 TDD 覆盖生成记录分页接口请求路径、请求体和响应解析。
- 主菜单和路由新增“生成记录”，路径 `/generation-records`，权限码 `admin:generation-record:view`；扩展路由测试覆盖入口权限声明。
- 生成记录页支持关键词、用户 ID、状态、记录类型和小程序筛选，展示用户 ID、身份 ID、小程序、客户端记录 ID、标题、类型、状态、摘要、创建时间和更新时间。
- 生成记录页状态选项按后端契约修正为 `draft`（待填写/草稿）、`generated`（已生成）、`expired`（已过期），移除不匹配的 `processing/success/failed`。
- 生成记录页记录类型按小程序本机 `caseType` 口径修正，覆盖 `private_lending`、`divorce_agreement`、`generic_template`、`contract_template`，未知值兜底显示原值。
- 新增 `src/pages/generation-records/generationRecordOptions.ts` 和测试，集中维护生成记录状态、tag 类型和记录类型展示映射。
- 分页选择限制在 `10/20/50/100`，前端请求时按后端约束将 `pageSize` 收敛到最大 100。
- 验证通过：`npm.cmd run quality` 通过，12 个测试文件、31 个 Vitest 测试通过，TypeScript 与 Vite 构建通过；构建保留既有 Rollup 注释 warning 和 chunk size warning。
- 新增法律表单事件后台页面 `/legal-form-events`，通过 `miniapp-backend` 的 `POST /api/admin/legal/form-events/page` 查询表单事件，不直连数据库。
- 新增 `src/api/legalFormEvents.ts`，封装法律表单事件分页接口；请求字段使用 `pageNo`、`pageSize`、`orderBy`、`order`、`appCode`、`formType`、`qualityStatus`、`keywords`，返回沿用 `dataList`、`totalCount`。
- 新增 `src/api/legalFormEvents.test.ts`，按 TDD 覆盖法律表单事件分页接口请求路径、请求体和响应解析。
- 主菜单和路由新增“法律表单事件”，路径 `/legal-form-events`，权限码 `admin:legal-form-event:view`；扩展路由测试覆盖入口权限声明。
- 法律表单事件页支持关键词、表单类型、质量状态和小程序筛选，展示分页表格、事件基础字段和 `payloadPreview` 预览列。
- 表单类型和质量状态筛选按当前小程序实际上报值配置，覆盖 `private_lending`、`divorce_agreement`、`generic_template` 和 `valid/low_value`。
- 关键词提示收敛为当前后端实际支持的客户端事件 ID、事件类型和 payload 内容，避免误导为可按用户 ID 搜索。
- 验证通过：`npm.cmd run quality` 通过，10 个测试文件、26 个 Vitest 测试通过，TypeScript 与 Vite 构建通过；构建保留既有 Rollup 注释 warning 和 chunk size warning。
- 同步平台基线 checkpoint：记录当前 HEAD `cf22873`，以及 `npm.cmd run quality` 通过，8 个测试文件、21 个 Vitest 测试通过，TypeScript 与 Vite 构建通过。
- 数据导入页从占位升级为本地预检工作台，支持选择 JSON 文件或粘贴 JSON 文本，在浏览器内解析记录数量、顶层字段完整度和敏感字段提醒。
- 新增 `src/pages/data-import/importPreflight.ts`，封装本地 JSON 预检纯函数；支持常见导出结构 `root[]`、`records[]`、`items[]`、`dataList[]`、`list[]` 和 `data[]`。
- 新增 `src/pages/data-import/importPreflight.test.ts`，覆盖记录数量、字段完整度、敏感字段检测和非法 JSON 状态。
- 数据导入页明确显示“仅本地预检，暂不上传”，当前不新增后端 API、不直连数据库、不控制 crawler。
- 验证通过：`npm.cmd run quality` 通过，9 个测试文件、24 个 Vitest 测试通过，TypeScript 与 Vite 构建通过；构建保留既有 Vite/Rollup warning 和 chunk size warning。

## 2026-05-21

- 初始化 `cekaitech-admin` 占位仓库。
- 新增项目级 `AGENTS.md`、`README.md`、checkpoint 文档和变更日志。
- 明确管理后台只通过 `miniapp-backend` 受控 API 管理业务数据，不直接写生产数据库，不直接控制本地 `crawler`。

## 2026-05-30

- 反馈编号：LMA-FB-013。
- 首页配置页功能入口编辑弹窗新增受控开源图标库选择器，替代自由输入 `图标 Key` 的主要操作路径。
- 图标选择器使用既有 `@element-plus/icons-vue` 开源图标组件做真实预览，不新增依赖；保存数据仍为后端既有 `iconKey` 字符串。
- 当前图标候选覆盖法律助手常见入口场景：计算、费用规则、流程清单、机构、援助、文书、复核、材料、查询、咨询、公告、管理、更多。
- 更新 `MiniappHomeConfigPage` 测试，覆盖图标库展示、真实图标预览节点和选择后回显。
- 验证记录：`npm.cmd run test -- --run src/pages/miniapp-home-config/MiniappHomeConfigPage.test.ts` 通过，1 个测试文件、4 个 Vitest 测试通过。
- 提交前验证：`npm.cmd run quality` 通过，23 个测试文件、98 个 Vitest 测试通过，`vue-tsc --noEmit && vite build` 通过；构建保留既有 Rollup PURE 注释 warning 和 chunk size warning。`git diff --check` 仅提示 Windows 换行转换。

## 2026-05-21

## 2026-06-12

- [反馈编号：无] 生产测试环境已部署结构化文书预览消费版本：`scripts/deploy-admin-static.ps1` 完成生产构建并同步 `/data/cekaitech-admin`，远端产物包含 `PrivateLendingResultTemplatePage-8DWK0qmb.js`，上一版已备份到 `/data/cekaitech-admin/.previous/`。
- [反馈编号：无] 线上验证通过：`https://admin.cekaitech.cn` 未携带 Basic Auth 返回 401；远端静态资源包含 `draftBlocks` 预览消费逻辑；阳律通 smoke 中 admin API 映射未登录返回 401。
- [反馈编号：无] 结果模板预览支持后端 `docPackage.draftBlocks` 结构化草稿块，优先按块类型渲染法律文书预览，缺失时继续回退解析 `draftContent`。
- [反馈编号：无] 后台 API 类型补充 `PrivateLendingDraftBlock`，覆盖 `type/text/align/indent` 字段，保持预览接口对新旧后端响应兼容。
- [反馈编号：无] 验证记录：先补充 API 和页面结构化块用例并确认页面未消费结构化块时失败；实现后运行 `npm.cmd run test -- --run src/api/privateLendingResultTemplate.test.ts src/pages/private-lending-result-template/PrivateLendingResultTemplatePage.test.ts` 通过，2 个文件 8 项测试通过。

# 2026-06-13

- [反馈编号：无] 结果模板配置页补齐劳动争议首片支持：劳动争议在案件类型选项中按可编辑模板展示，支持加载、编辑和预览后端 `labor` 结果模板。
- [反馈编号：无] 管理端预览样例新增劳动者、用人单位、用工日期、争议类型、主张金额和证据状态字段，避免切换劳动争议后仍使用民间借贷样例。
- [反馈编号：无] 验证记录：RED 阶段 `npm.cmd test -- PrivateLendingResultTemplatePage.test.ts` 失败于 labor 预览仍提交借贷样例；GREEN 后同命令通过 8 项。

- 确认 `cekaitech-admin` 首版采用后台框架优先策略。
# 2026-06-18

- [反馈编号：无] 律师认证审核页默认展示全部审核状态，避免已通过或已驳回记录因默认 `pending` 筛选被隐藏；用户仍可通过状态下拉筛选待审核记录。
- [反馈编号：无] 失信/限高查询任务页适配审核入队状态流：新增 `待审核(pending_review)` 和 `已拒绝(rejected)` 状态展示、筛选和标签映射。
- [反馈编号：无] 失信/限高查询任务页新增后台审核操作：具备 `admin:legal-credit-query:manage` 权限时，可对待审核任务执行“审核通过”进入队列，或“拒绝”阻止 crawler 消费。
- [反馈编号：无] 验证记录：补充 API 与页面测试覆盖 approve/reject 调用和待审核操作按钮。

- 确认技术栈为 Vue 3 + Vite + TypeScript + Element Plus。
- 新增 `docs/admin-mvp-design.md`，明确首批页面、目录结构、数据边界、权限阶段和暂不做范围。
- 更新 checkpoint 文档，记录当前处于设计复核阶段。

## 2026-05-21

- [反馈编号：无] 年度覆盖矩阵生产静态部署：`scripts\deploy-admin-static.ps1` 构建通过并上传 `DataGovernancePage-qqL96yll.js` 到 `/data/cekaitech-admin/assets/`；公网 `admin.cekaitech.cn` 未登录返回 `401`，服务器静态资源包含 `annual-common-data/coverage-matrix`，可与生产测试 backend 新接口联调。
- 新增 `docs/superpowers/plans/2026-05-21-admin-mvp-shell.md`，作为 `cekaitech-admin` MVP 后台壳的实现计划。

## 2026-05-21

- 初始化 Vue 3 + Vite + TypeScript + Element Plus 后台壳。
- 新增登录占位、后台主布局、左侧菜单、应用/工作区切换和首页工作台。
- 新增用户管理、限制与黑名单、律师认证审核、数据导入、系统设置占位页面。
- 新增路由、mock 登录状态、工作区状态和基础测试。
- 新增本地开发、测试、构建和质量检查命令说明。

## 2026-05-31

- [反馈编号：LMA-FB-027] 法律工具中心测试样例补充“要素式示范文本”能力与曝光入口，覆盖 `element_template_library`、`file-check-2`、`/pages/element-template/element-template` 和 `backend_data` 管理后台展示链路。

## 2026-05-22

- 新增前端权限判断能力：`auth.hasPermission(permissionCode)` 基于当前管理员权限数组判断可见性。
- 左侧全局菜单新增 `permissionCode`，通过 `filterAdminMenuItems` 按权限过滤。
- 后台路由新增 `meta.permissionCode`，路由守卫在已登录但无权限时重定向到 `/dashboard`。
- 工作区菜单新增 `filterWorkspaceMenus`，按后端返回的 `permissionCode` 过滤当前可见工作区功能。
- 用户管理页按权限控制“生成演示数据”和“状态调整”按钮：分别对应 `admin:dev:seed` 与 `admin:user:status:update`。
- 限制与黑名单页按 `admin:user-restriction:manage` 控制“新增限制”和“取消”按钮。
- 扩展 auth、router、workspace 测试，覆盖权限判断、全局菜单过滤、路由权限声明和工作区菜单过滤。
- 验证通过：`npm.cmd run quality` 通过，21 个 Vitest 测试通过，TypeScript 与 Vite 构建通过。
- 系统设置页新增当前管理员改密表单，调用 `POST /api/admin/auth/change-password`。
- 新增 `changeAdminPassword` API 封装和 `src/api/adminAuth.test.ts`，覆盖改密请求携带 Bearer token。
- 改密成功后清理本地后台 token 并跳转登录页，要求管理员使用新密码重新登录。
- 更新 `codex-decisions.md`，记录改密成功后强制重新登录的决策。
- 验证通过：`npm.cmd run quality` 通过，17 个 Vitest 测试通过，TypeScript 与 Vite 构建通过。
- 新增 `.env.production.example`，生产构建默认指向 `https://api.cekaitech.cn`。
- 新增 `docs/production-runbook.md`，记录 `admin.cekaitech.cn` 静态部署、Nginx、验证、回滚和安全边界。
- 更新 `codex-decisions.md`，记录管理后台首版按 Nginx 静态站部署的决策。
- 更新 README，补充生产环境变量和部署文档入口。
- 通用请求封装新增后台登录失效处理：非登录接口收到 HTTP 401 时清理 `cekaitech-admin-token`，并派发 `cekaitech-admin:unauthorized` 事件。
- 应用入口监听登录失效事件，当前不在登录页时自动跳转 `/login`，避免后台全局鉴权落地后用户停留在失效会话页面。
- 新增 `src/api/http.test.ts`，覆盖受保护接口 401 清理 token 和登录接口 401 不触发全局失效处理。
- 验证通过：`npm.cmd run quality` 通过，16 个 Vitest 测试通过，TypeScript 与 Vite 构建通过。
- 通用请求封装 `src/api/http.ts` 新增后台 token 自动注入：本地存在 `cekaitech-admin-token` 时，除登录接口外自动附带 `Authorization: Bearer <token>`。
- 用户状态调整请求会携带登录 token，支撑 `miniapp-backend` 将真实后台管理员写入用户操作审计日志。
- 扩展 `src/api/adminUsers.test.ts`，覆盖状态调整请求携带 Authorization 头。
- 验证通过：`npm.cmd run quality` 通过，14 个 Vitest 测试通过，TypeScript 与 Vite 构建通过。
- 新增 `src/api/adminUserOperationLogs.ts`，封装 `POST /api/admin/user-operation-logs/page` 后台用户操作日志分页接口。
- 用户管理详情抽屉新增“操作记录”区域，进入详情后加载用户最近状态调整记录，状态调整成功后自动刷新记录。
- 新增 `src/api/adminUserOperationLogs.test.ts`，覆盖操作日志分页接口请求。
- 验证通过：`npm.cmd run quality` 通过，14 个 Vitest 测试通过，TypeScript 与 Vite 构建通过。
- 用户管理页新增“调整状态”操作，可将用户设为正常、受限或黑名单，并要求填写原因。
- 新增 `updateAdminUserStatus` API 封装，调用 `POST /api/admin/users/update-status` 并刷新用户列表/详情。
- 扩展 `src/api/adminUsers.test.ts`，覆盖用户状态调整接口请求。
- 验证通过：`npm.cmd run quality` 通过，13 个 Vitest 测试通过，TypeScript 与 Vite 构建通过。
- 限制与黑名单页面从占位页升级为用户限制管理页面，调用 `miniapp-backend` 用户限制接口。
- 新增 `src/api/adminUserRestrictions.ts`，封装限制记录分页、创建和取消接口。
- 限制管理页支持按用户ID、小程序、限制类型和状态筛选，支持新增全局/指定小程序限制，支持取消生效中限制。
- 新增 `src/api/adminUserRestrictions.test.ts`，覆盖分页、创建和取消接口请求。
- 验证通过：`npm.cmd run quality` 通过，12 个 Vitest 测试通过，TypeScript 与 Vite 构建通过。
- 用户管理页新增“生成演示数据”按钮，调用 `POST /api/admin/dev/seed-users` 后刷新列表，便于本地联调空库场景。
- 用户管理页新增详情抽屉，调用 `GET /api/admin/users/{userId}/detail` 展示用户基础信息、小程序身份列表和手机号记录。
- 扩展 `src/api/adminUsers.ts` 和 `src/api/adminUsers.test.ts`，覆盖用户分页、用户详情和演示数据生成接口。
- 验证通过：`npm.cmd run quality` 通过，10 个 Vitest 测试通过，TypeScript 与 Vite 构建通过。
- 用户管理页从占位页升级为真实 API 页面，调用 `POST /api/admin/users/page` 加载统一用户列表。
- 新增 `src/api/adminUsers.ts`，封装后台用户分页查询接口和分页返回结构。
- 用户管理页支持关键词、用户状态和来源小程序筛选，并展示手机号、`unionId`、`openid`、手机号授权、角色和创建时间。
- 新增 `src/api/adminUsers.test.ts`，覆盖用户分页 API 的请求路径、请求体和分页响应解析。
- 前端登录从纯 mock store 改为调用 `miniapp-backend` 的后台开发态登录接口。
- 新增 `src/api/http.ts` 和 `src/api/adminAuth.ts`，统一封装后台 API 请求和管理员认证接口。
- 登录成功后保存 `cekaitech-admin-token`，登出时清理本地 token。
- Vite 开发服务新增 `/api -> http://127.0.0.1:8080` 代理，便于本地前后端联调。
- 更新 auth store 测试，覆盖 API 登录成功和登录失败提示。
- 工作区选择从前端固定数组升级为调用 `GET /api/admin/workspaces`，接口异常时保留前端兜底工作区。
- 新增 `src/api/adminWorkspace.ts`，封装工作区列表和工作区菜单接口。
- 切换工作区后调用 `GET /api/admin/workspaces/{workspaceId}/menus` 并在侧边栏展示工作区菜单区。
- 新增 workspace store 测试，覆盖工作区加载、兜底列表和菜单加载。
- 新增动态工作区菜单路由 `/workspace-menu/:workspaceCode/:menuCode`，侧边栏工作区菜单可点击进入通用功能占位页。
- 新增 `WorkspaceMenuPage`，展示工作区编码、菜单编码、后端路由和权限码，作为后续真实业务页面接入前的稳定落点。
